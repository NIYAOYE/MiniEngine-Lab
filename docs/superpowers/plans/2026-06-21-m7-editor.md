# M7 Editor as Client Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用引擎自己的 ImGui 编辑器作为 M6 Tool API 的第一个客户端,反向验证 6 个 Tool 足以驱动一个"实体增删 + 变换编辑 + 层级浏览 + 审计查看"的最小编辑器。

**Architecture:** 新建 CPU-only 的 `me_editor` 静态库,内含 `EditorController`——**只经 `ToolRegistry::Invoke` 读写引擎**(读走查询 Tool、写走变更 Tool),在 WSL doctest 红绿。ImGui 与 DX12 后端封死在 sandbox 边界(目视层,不单测),面板只调 controller 意图方法。

**Tech Stack:** C++17、nlohmann/json(Tool 边界)、doctest(单测)、Dear ImGui v1.91.5(sandbox 目视)、DX12 + Win32(sandbox 后端)。

## Global Constraints

- C++ 标准:C++17(`CMAKE_CXX_STANDARD 17`,无扩展)。每个 target `target_compile_features(... cxx_std_17)`。
- 不使用 C++ 异常:可恢复失败用返回值 / `ToolResult` / `CommandResult` / 布尔;不变量用 `ME_ASSERT`。**nlohmann `.at()` 会抛异常,禁止用于解析路径**;一律 `contains()` 守卫后 `operator[]`/`.value()`。
- 无硬编码魔法数字:常量用具名 `constexpr`。
- 公开 API 写 Doxygen 注释;非显而易见实现写行内注释;头文件禁 `using namespace std`。
- 模块依赖严格单向:`me_editor → me_toolapi → me_command → me_scene → me_core` + `nlohmann_json`。**`me_editor` 严禁依赖 ImGui / RHI / renderer / platform**。
- MSVC 必须 `/utf-8`(源文件含中文注释),已由根 CMake 全局设置。
- 编辑期对场景的变更必须经 Tool API + Command;EditorController 固定 `CallerRole::Editor`。
- 新增模块必须同步更新 `CMakeLists.txt` 与模块 README。

---

## File Structure

| 文件 | 责任 |
|------|------|
| `engine/editor/include/me/editor/EditorDto.h` | DTO:`EntityRow` / `EntityDetails` / `LogRow`(纯数据,无逻辑) |
| `engine/editor/include/me/editor/EditorController.h` | EditorController 接口(全部意图方法声明) |
| `engine/editor/src/EditorController.cpp` | 实现 + 匿名命名空间内 JSON→DTO 解析辅助 |
| `engine/editor/CMakeLists.txt` | `me_editor` STATIC 库 |
| `engine/editor/README.md` | 模块说明 |
| `tests/editor/test_editor_controller.cpp` | EditorController doctest(全 CPU-only) |
| `third_party/CMakeLists.txt`(改) | 追加 Dear ImGui(URL 钉版,自建 imgui STATIC) |
| `engine/platform/include/me/platform/Window.h`(改) | 新增 Win32 消息钩子注册 |
| `engine/platform/src/*Window*.cpp`(改) | WndProc 调用钩子 |
| `sandbox/CMakeLists.txt`(改) | 链接 me_editor / me_toolapi / me_command / imgui |
| `sandbox/main.cpp`(改) | ImGui 初始化 + 每帧接入 + 四面板绘制 |
| `docs/architecture/0005-m7-editor.md` | ADR |
| `docs/PROGRESS.md`(改) | 进度回写 |

测试注册:`tests/CMakeLists.txt` 在 `add_executable(me_tests ...)` 列表追加 `editor/test_editor_controller.cpp`,并在 `target_link_libraries(me_tests ...)` 追加 `me_editor`。

---

## Task 1: me_editor 库骨架 + DTO + invoke 助手 + RefreshHierarchy

**Files:**
- Create: `engine/editor/include/me/editor/EditorDto.h`
- Create: `engine/editor/include/me/editor/EditorController.h`
- Create: `engine/editor/src/EditorController.cpp`
- Create: `engine/editor/CMakeLists.txt`
- Create: `engine/editor/README.md`
- Create: `tests/editor/test_editor_controller.cpp`
- Modify: `CMakeLists.txt`(在 `add_subdirectory(engine/renderer)` 后加 `add_subdirectory(engine/editor)`)
- Modify: `tests/CMakeLists.txt`(源列表 + link 追加,见上)

**Interfaces:**
- Consumes: `me::toolapi::ToolRegistry::Invoke(name, params, CallerRole, ToolContext&, dryRun=false) -> ToolResult`;`ToolContext{scene, commands, log}`;`me::toolapi::CallerRole::Editor`;`me::toolapi::ToString(ToolErrorCode)`;`scene.list_entities` 返回 `{count, entities:[{id, position:{x,y}, rotation, scale:{x,y}}]}`。
- Produces:
  - DTO:`me::editor::EntityRow{ me::scene::EntityId id; me::Transform2D localTransform; }`、`EntityDetails{ me::scene::EntityId id; me::Transform2D localTransform; me::scene::EntityId parentId; std::vector<me::scene::EntityId> children; }`、`LogRow{ std::uint64_t invocationId; std::string toolName; bool ok; std::string code; }`。
  - `class EditorController`,构造 `EditorController(me::toolapi::ToolRegistry&, me::toolapi::ToolContext&)`;`static constexpr me::scene::EntityId kInvalidEntityId = 0;`
  - 本任务实现:`void RefreshHierarchy()`、`const std::vector<EntityRow>& Hierarchy() const`、`const std::string& LastError() const`、`bool HasError() const`、`void ClearError()`。
  - 头文件**声明全部**后续方法(`Select`/`Selected`/`HasSelection`/`InspectSelected`/`Inspected`/`HasInspected`/`CreateEntity`/`DestroySelected`/`ApplyTransform`/`Undo`/`Redo`/`CanUndo`/`CanRedo`/`RefreshLog`/`Log`),供后续任务实现(静态库未引用符号不报错)。

- [ ] **Step 1: 写 DTO 头文件**

`engine/editor/include/me/editor/EditorDto.h`:
```cpp
#pragma once

#include <cstdint>
#include <string>
#include <vector>

#include "me/core/Transform2D.h"
#include "me/scene/Scene.h" // me::scene::EntityId

namespace me::editor {

/// @brief Hierarchy 面板一行:来自 scene.list_entities 的单个实体。
struct EntityRow {
    me::scene::EntityId id = 0;
    me::Transform2D localTransform;
};

/// @brief Inspector 详情:来自 scene.get_entity(本切片无组件列表,见 spec §5)。
struct EntityDetails {
    me::scene::EntityId id = 0;
    me::Transform2D localTransform;
    me::scene::EntityId parentId = 0;
    std::vector<me::scene::EntityId> children;
};

/// @brief Log 面板一行:来自 log.read 的单条审计记录(取四字段)。
struct LogRow {
    std::uint64_t invocationId = 0;
    std::string toolName;
    bool ok = true;
    std::string code;
};

} // namespace me::editor
```

- [ ] **Step 2: 写 EditorController 头文件**

`engine/editor/include/me/editor/EditorController.h`:
```cpp
#pragma once

#include <string>
#include <vector>

#include <nlohmann/json.hpp>

#include "me/core/Transform2D.h"
#include "me/editor/EditorDto.h"
#include "me/scene/Scene.h"
#include "me/toolapi/ToolContext.h"
#include "me/toolapi/ToolRegistry.h"
#include "me/toolapi/ToolResult.h"

namespace me::editor {

/**
 * @brief 编辑器决策核心:把 UI 意图翻译为 Tool 调用,只经 ToolRegistry 读写引擎。
 *
 * CPU-only、不依赖 ImGui/RHI,故可在 WSL 单测。读走查询 Tool、写走变更 Tool;
 * Undo/Redo 因 M6 未暴露为 Tool,作为特权宿主直调 CommandStack(见 spec §5)。
 */
class EditorController {
public:
    /// @brief 无效实体哨兵(无选中)。
    static constexpr me::scene::EntityId kInvalidEntityId = 0;

    /// @brief 注入受控门面;controller 不持有所有权。
    EditorController(me::toolapi::ToolRegistry& registry, me::toolapi::ToolContext& ctx);

    // —— 查询(经查询 Tool)——
    /// @brief 经 scene.list_entities 刷新 Hierarchy 缓存。
    void RefreshHierarchy();
    const std::vector<EntityRow>& Hierarchy() const { return m_hierarchy; }

    /// @brief 选中实体(本地状态)。
    void Select(me::scene::EntityId id);
    me::scene::EntityId Selected() const { return m_selected; }
    bool HasSelection() const { return m_selected != kInvalidEntityId; }
    /// @brief 经 scene.get_entity 刷新选中实体详情。
    void InspectSelected();
    const EntityDetails& Inspected() const { return m_inspected; }
    bool HasInspected() const { return m_hasInspected; }

    /// @brief 经 log.read 刷新审计日志缓存。
    void RefreshLog();
    const std::vector<LogRow>& Log() const { return m_log; }

    // —— 变更(经变更 Tool)——
    /// @brief 经 scene.create_entity 创建实体,成功后选中并刷新。
    void CreateEntity();
    /// @brief 经 scene.destroy_entity 销毁选中实体,成功后清空选中并刷新。
    void DestroySelected();
    /// @brief 经 entity.set_transform 覆盖选中实体的局部变换。
    void ApplyTransform(const me::Transform2D& t);

    // —— 命令栈元操作(M6 无对应 Tool,特权宿主直调,见 spec §5)——
    void Undo();
    void Redo();
    bool CanUndo() const;
    bool CanRedo() const;

    // —— 错误暴露(绝不静默)——
    const std::string& LastError() const { return m_lastError; }
    bool HasError() const { return !m_lastError.empty(); }
    void ClearError() { m_lastError.clear(); }

private:
    /// @brief 统一 Invoke:失败写 m_lastError、成功清空;以 Editor 角色调用。
    me::toolapi::ToolResult invoke(const std::string& name, const nlohmann::json& params);
    /// @brief 刷新后核对选中是否仍存活;失效则清空选中与详情。
    void reconcileSelection();

    me::toolapi::ToolRegistry& m_registry;
    me::toolapi::ToolContext& m_ctx;

    std::vector<EntityRow> m_hierarchy;
    me::scene::EntityId m_selected = kInvalidEntityId;
    EntityDetails m_inspected;
    bool m_hasInspected = false;
    std::vector<LogRow> m_log;
    std::string m_lastError;
};

} // namespace me::editor
```

- [ ] **Step 3: 写失败测试**

`tests/editor/test_editor_controller.cpp`:
```cpp
#include <doctest/doctest.h>

#include "me/command/CommandStack.h"
#include "me/editor/EditorController.h"
#include "me/scene/Scene.h"
#include "me/toolapi/ToolContext.h"
#include "me/toolapi/ToolRegistry.h"
#include "me/toolapi/tools/BuiltinTools.h"

using namespace me::editor;

namespace {
// 测试夹具:registry(含 6 builtin) + scene + stack + log + ctx + controller。
struct Fixture {
    me::toolapi::ToolRegistry registry;
    me::scene::Scene scene;
    me::command::CommandStack stack;
    me::toolapi::ToolInvocationLog log;
    me::toolapi::ToolContext ctx{scene, stack, log};
    EditorController ctrl{registry, ctx};
    Fixture() { me::toolapi::RegisterBuiltinTools(registry); }
    // 经 create_entity Tool 直接造一个实体,返回其持久 id。
    me::scene::EntityId makeEntity() {
        auto r = registry.Invoke("scene.create_entity", nlohmann::json::object(),
                                 me::toolapi::CallerRole::Editor, ctx);
        REQUIRE(r.ok);
        return r.data["id"].get<me::scene::EntityId>();
    }
};
} // namespace

TEST_CASE("EditorController:空场景 RefreshHierarchy 得空列表、无错误") {
    Fixture f;
    f.ctrl.RefreshHierarchy();
    CHECK(f.ctrl.Hierarchy().empty());
    CHECK_FALSE(f.ctrl.HasError());
}

TEST_CASE("EditorController:RefreshHierarchy 反映已存在实体的身份与变换") {
    Fixture f;
    const auto id = f.makeEntity();
    f.ctrl.RefreshHierarchy();
    REQUIRE(f.ctrl.Hierarchy().size() == 1);
    CHECK(f.ctrl.Hierarchy()[0].id == id);
    // 新建实体为单位变换:scale 默认 1。
    CHECK(f.ctrl.Hierarchy()[0].localTransform.scale.x == doctest::Approx(1.0f));
}
```

- [ ] **Step 4: 运行测试,确认失败(链接/编译错误即可)**

Run: `cmake --build build --target me_tests`
Expected: 编译/链接失败(`me_editor` 尚无实现 / 符号未定义)。

- [ ] **Step 5: 写 CMake + README + 实现**

`engine/editor/CMakeLists.txt`:
```cmake
add_library(me_editor STATIC
    src/EditorController.cpp
)
target_include_directories(me_editor PUBLIC include)
target_compile_features(me_editor PUBLIC cxx_std_17)
# 单向依赖:editor → toolapi → command → scene → core。严禁依赖 rhi / renderer / platform / imgui。
# nlohmann/json 出现在解析路径(非公开头),但 DTO 头不含 json → 仅 PRIVATE 即可;
# 然 ToolContext.h/ToolRegistry.h(PUBLIC 头引用)已 PUBLIC 传递 json,故用 PUBLIC 链接 toolapi。
target_link_libraries(me_editor PUBLIC me_toolapi me_command me_scene me_core nlohmann_json::nlohmann_json)
```

`engine/editor/README.md`:
```markdown
# me_editor — Editor as Client(M7)

CPU-only 编辑器决策核心。`EditorController` 把 UI 意图翻译为 M6 Tool 调用,
**只经 `ToolRegistry::Invoke` 读写引擎**(读走查询 Tool、写走变更 Tool),
以此反向验证 Tool API 完备性。不依赖 ImGui/RHI,可在 WSL doctest 红绿;
ImGui 面板是 sandbox 的目视层,不在本库内。

依赖:editor → toolapi → command → scene → core + nlohmann/json。
```

`engine/editor/src/EditorController.cpp`(本任务先实现 ctor / invoke / RefreshHierarchy + 解析辅助;后续任务追加其余方法到本文件):
```cpp
#include "me/editor/EditorController.h"

namespace me::editor {
namespace {

// 从 {position:{x,y}, rotation, scale:{x,y}} 读 Transform2D;字段缺失返回 false。
// 不用 nlohmann .at()(会抛异常,违反项目规范),一律 contains 守卫。
bool ReadTransform(const nlohmann::json& j, me::Transform2D& out) {
    if (!j.contains("position") || !j.contains("rotation") || !j.contains("scale"))
        return false;
    const nlohmann::json& pos = j["position"];
    const nlohmann::json& sc = j["scale"];
    if (!pos.contains("x") || !pos.contains("y") || !sc.contains("x") || !sc.contains("y"))
        return false;
    out.position.x = pos["x"].get<float>();
    out.position.y = pos["y"].get<float>();
    out.rotation = j["rotation"].get<float>();
    out.scale.x = sc["x"].get<float>();
    out.scale.y = sc["y"].get<float>();
    return true;
}

} // namespace

EditorController::EditorController(me::toolapi::ToolRegistry& registry,
                                  me::toolapi::ToolContext& ctx)
    : m_registry(registry), m_ctx(ctx) {}

me::toolapi::ToolResult EditorController::invoke(const std::string& name,
                                                const nlohmann::json& params) {
    me::toolapi::ToolResult r =
        m_registry.Invoke(name, params, me::toolapi::CallerRole::Editor, m_ctx);
    if (!r.ok)
        m_lastError = std::string(me::toolapi::ToString(r.code)) + ": " + r.message;
    else
        m_lastError.clear();
    return r;
}

void EditorController::RefreshHierarchy() {
    m_hierarchy.clear();
    const me::toolapi::ToolResult r = invoke("scene.list_entities", nlohmann::json::object());
    if (!r.ok || !r.data.contains("entities")) return;
    for (const nlohmann::json& e : r.data["entities"]) {
        EntityRow row;
        if (!e.contains("id")) continue;
        row.id = e["id"].get<me::scene::EntityId>();
        ReadTransform(e, row.localTransform); // 失败保留默认单位变换
        m_hierarchy.push_back(row);
    }
}

} // namespace me::editor
```

在 `CMakeLists.txt`(根)`add_subdirectory(engine/renderer)` 之后插入:
```cmake
add_subdirectory(engine/editor)
```

在 `tests/CMakeLists.txt` 的 `add_executable(me_tests ...)` 源列表末尾(`toolapi/test_builtin_integration.cpp` 后)追加:
```cmake
    editor/test_editor_controller.cpp
```
并把 `target_link_libraries(me_tests PRIVATE ... me_toolapi)` 改为追加 `me_editor`:
```cmake
target_link_libraries(me_tests PRIVATE doctest::doctest me_core me_platform me_rhi_cpu me_assets me_scene me_command me_toolapi me_editor)
```

- [ ] **Step 6: 运行测试,确认通过**

Run: `cmake --build build --target me_tests && ./build/bin/me_tests -ts="*" -tc="EditorController:*"`
Expected: 新增 2 个用例 PASS,既有用例不回归。

- [ ] **Step 7: 提交**

```bash
git add engine/editor tests/editor tests/CMakeLists.txt CMakeLists.txt
git commit -m "feat(editor): me_editor 库 + EditorController.RefreshHierarchy(经 scene.list_entities)"
```

---

## Task 2: Select + InspectSelected + 错误暴露

**Files:**
- Modify: `engine/editor/src/EditorController.cpp`(追加 `Select` / `InspectSelected`)
- Test: `tests/editor/test_editor_controller.cpp`

**Interfaces:**
- Consumes:`scene.get_entity` 入参 `{id}`,返回 `{id, position, rotation, scale, parentId, children}` 或 `PreconditionFailed`(实体不存在)。
- Produces:`void Select(me::scene::EntityId)`、`void InspectSelected()`、详情读 `Inspected()` / `HasInspected()`;非法 id 经 invoke 设 `LastError()`。

- [ ] **Step 1: 写失败测试**

追加到 `tests/editor/test_editor_controller.cpp`:
```cpp
TEST_CASE("EditorController:Select+InspectSelected 得到变换与层级") {
    Fixture f;
    const auto id = f.makeEntity();
    // 经 Tool 设一个非平凡变换,确认 inspect 能读回。
    f.registry.Invoke("entity.set_transform",
                      {{"id", id}, {"position", {{"x", 7.0}, {"y", 9.0}}}},
                      me::toolapi::CallerRole::Editor, f.ctx);
    f.ctrl.Select(id);
    f.ctrl.InspectSelected();
    REQUIRE(f.ctrl.HasInspected());
    CHECK(f.ctrl.Inspected().id == id);
    CHECK(f.ctrl.Inspected().localTransform.position.x == doctest::Approx(7.0f));
    CHECK(f.ctrl.Inspected().parentId == 0); // 无父
    CHECK(f.ctrl.Inspected().children.empty());
    CHECK_FALSE(f.ctrl.HasError());
}

TEST_CASE("EditorController:InspectSelected 不存在实体 → LastError,不崩") {
    Fixture f;
    f.ctrl.Select(9999); // 从未创建
    f.ctrl.InspectSelected();
    CHECK_FALSE(f.ctrl.HasInspected());
    CHECK(f.ctrl.HasError()); // 经 get_entity PreconditionFailed
}
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cmake --build build --target me_tests`
Expected: 链接失败(`Select`/`InspectSelected` 未定义)。

- [ ] **Step 3: 实现 Select + InspectSelected**

在 `EditorController.cpp` 的 `namespace me::editor {` 内追加:
```cpp
void EditorController::Select(me::scene::EntityId id) { m_selected = id; }

void EditorController::InspectSelected() {
    m_hasInspected = false;
    if (!HasSelection()) return;
    const me::toolapi::ToolResult r = invoke("scene.get_entity", {{"id", m_selected}});
    if (!r.ok) return;
    EntityDetails d;
    d.id = m_selected;
    if (!ReadTransform(r.data, d.localTransform)) {
        m_lastError = "malformed get_entity response";
        return;
    }
    d.parentId = r.data.value("parentId", me::scene::EntityId{0});
    if (r.data.contains("children"))
        for (const nlohmann::json& c : r.data["children"])
            d.children.push_back(c.get<me::scene::EntityId>());
    m_inspected = d;
    m_hasInspected = true;
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cmake --build build --target me_tests && ./build/bin/me_tests -tc="EditorController:*"`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/editor/src/EditorController.cpp tests/editor/test_editor_controller.cpp
git commit -m "feat(editor): Select+InspectSelected(经 scene.get_entity)+ 错误暴露"
```

---

## Task 3: CreateEntity

**Files:**
- Modify: `engine/editor/src/EditorController.cpp`
- Test: `tests/editor/test_editor_controller.cpp`

**Interfaces:**
- Consumes:`scene.create_entity` 入参 `{}`,返回 `{id}`。
- Produces:`void CreateEntity()`——成功后刷新 Hierarchy、选中新实体并 InspectSelected。

- [ ] **Step 1: 写失败测试**

```cpp
TEST_CASE("EditorController:CreateEntity 实体+1、选中新实体、Hierarchy 刷新") {
    Fixture f;
    f.ctrl.CreateEntity();
    REQUIRE(f.ctrl.Hierarchy().size() == 1);
    CHECK(f.ctrl.HasSelection());
    CHECK(f.ctrl.Selected() == f.ctrl.Hierarchy()[0].id);
    CHECK(f.ctrl.HasInspected());
    CHECK_FALSE(f.ctrl.HasError());
}
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cmake --build build --target me_tests`
Expected: 链接失败(`CreateEntity` 未定义)。

- [ ] **Step 3: 实现**

```cpp
void EditorController::CreateEntity() {
    const me::toolapi::ToolResult r = invoke("scene.create_entity", nlohmann::json::object());
    if (!r.ok) return;
    const auto id = r.data.value("id", me::scene::EntityId{0});
    RefreshHierarchy();
    if (id != kInvalidEntityId) {
        m_selected = id;
        InspectSelected();
    }
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cmake --build build --target me_tests && ./build/bin/me_tests -tc="EditorController:*"`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/editor/src/EditorController.cpp tests/editor/test_editor_controller.cpp
git commit -m "feat(editor): CreateEntity(经 scene.create_entity,创建后选中)"
```

---

## Task 4: ApplyTransform

**Files:**
- Modify: `engine/editor/src/EditorController.cpp`
- Test: `tests/editor/test_editor_controller.cpp`

**Interfaces:**
- Consumes:`entity.set_transform` 入参 `{id, position:{x,y}, rotation, scale:{x,y}}`,返回 `{id}`。
- Produces:`void ApplyTransform(const me::Transform2D&)`——无选中设 LastError;成功后重新 InspectSelected。

- [ ] **Step 1: 写失败测试**

```cpp
TEST_CASE("EditorController:ApplyTransform 写入并经 inspect 读回") {
    Fixture f;
    f.ctrl.CreateEntity();
    me::Transform2D t;
    t.position = me::Vector2{12.0f, -3.0f};
    t.rotation = 1.5f;
    t.scale = me::Vector2{2.0f, 2.0f};
    f.ctrl.ApplyTransform(t);
    REQUIRE(f.ctrl.HasInspected());
    CHECK(f.ctrl.Inspected().localTransform.position.x == doctest::Approx(12.0f));
    CHECK(f.ctrl.Inspected().localTransform.rotation == doctest::Approx(1.5f));
    CHECK(f.ctrl.Inspected().localTransform.scale.y == doctest::Approx(2.0f));
    CHECK_FALSE(f.ctrl.HasError());
}

TEST_CASE("EditorController:ApplyTransform 无选中 → LastError") {
    Fixture f;
    f.ctrl.ApplyTransform(me::Transform2D{});
    CHECK(f.ctrl.HasError());
}
```
(需在测试文件顶部 `#include "me/core/Vector2.h"`。)

- [ ] **Step 2: 运行测试,确认失败**

Run: `cmake --build build --target me_tests`
Expected: 链接失败(`ApplyTransform` 未定义)。

- [ ] **Step 3: 实现**

```cpp
void EditorController::ApplyTransform(const me::Transform2D& t) {
    if (!HasSelection()) {
        m_lastError = "ApplyTransform: no selection";
        return;
    }
    const nlohmann::json params = {
        {"id", m_selected},
        {"position", {{"x", t.position.x}, {"y", t.position.y}}},
        {"rotation", t.rotation},
        {"scale", {{"x", t.scale.x}, {"y", t.scale.y}}},
    };
    const me::toolapi::ToolResult r = invoke("entity.set_transform", params);
    if (!r.ok) return;
    InspectSelected();
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cmake --build build --target me_tests && ./build/bin/me_tests -tc="EditorController:*"`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/editor/src/EditorController.cpp tests/editor/test_editor_controller.cpp
git commit -m "feat(editor): ApplyTransform(经 entity.set_transform)"
```

---

## Task 5: DestroySelected

**Files:**
- Modify: `engine/editor/src/EditorController.cpp`
- Test: `tests/editor/test_editor_controller.cpp`

**Interfaces:**
- Consumes:`scene.destroy_entity` 入参 `{id}`,返回 `{id}`(EditorOnly 权限,Editor 角色可调)。
- Produces:`void DestroySelected()`——无选中设 LastError;成功后清空选中/详情并刷新 Hierarchy。

- [ ] **Step 1: 写失败测试**

```cpp
TEST_CASE("EditorController:DestroySelected 实体消失、选中清空") {
    Fixture f;
    f.ctrl.CreateEntity();
    REQUIRE(f.ctrl.Hierarchy().size() == 1);
    f.ctrl.DestroySelected();
    CHECK(f.ctrl.Hierarchy().empty());
    CHECK_FALSE(f.ctrl.HasSelection());
    CHECK_FALSE(f.ctrl.HasInspected());
    CHECK_FALSE(f.ctrl.HasError());
}

TEST_CASE("EditorController:DestroySelected 无选中 → LastError") {
    Fixture f;
    f.ctrl.DestroySelected();
    CHECK(f.ctrl.HasError());
}
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cmake --build build --target me_tests`
Expected: 链接失败(`DestroySelected` 未定义)。

- [ ] **Step 3: 实现**

```cpp
void EditorController::DestroySelected() {
    if (!HasSelection()) {
        m_lastError = "DestroySelected: no selection";
        return;
    }
    const me::toolapi::ToolResult r = invoke("scene.destroy_entity", {{"id", m_selected}});
    if (!r.ok) return;
    m_selected = kInvalidEntityId;
    m_hasInspected = false;
    RefreshHierarchy();
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cmake --build build --target me_tests && ./build/bin/me_tests -tc="EditorController:*"`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/editor/src/EditorController.cpp tests/editor/test_editor_controller.cpp
git commit -m "feat(editor): DestroySelected(经 scene.destroy_entity,清空选中)"
```

---

## Task 6: Undo / Redo + 选中核对

**Files:**
- Modify: `engine/editor/src/EditorController.cpp`
- Test: `tests/editor/test_editor_controller.cpp`

**Interfaces:**
- Consumes:`me::command::CommandStack::undo(Scene&) -> CommandResult{ok,message}`、`redo(Scene&)`、`canUndo()`、`canRedo()`(经 `m_ctx.commands` / `m_ctx.scene`)。
- Produces:`void Undo()`、`void Redo()`、`bool CanUndo() const`、`bool CanRedo() const`;均刷新 Hierarchy 并经 `reconcileSelection()` 核对选中存活。

- [ ] **Step 1: 写失败测试**

```cpp
TEST_CASE("EditorController:Undo 撤销 create → 实体消失、选中失效被清") {
    Fixture f;
    f.ctrl.CreateEntity();
    REQUIRE(f.ctrl.Hierarchy().size() == 1);
    REQUIRE(f.ctrl.CanUndo());
    f.ctrl.Undo();
    CHECK(f.ctrl.Hierarchy().empty());
    CHECK_FALSE(f.ctrl.HasSelection()); // 被撤销实体不再存活 → 选中清空
    CHECK_FALSE(f.ctrl.HasError());
    CHECK(f.ctrl.CanRedo());
}

TEST_CASE("EditorController:Redo 重做 create → 实体回来") {
    Fixture f;
    f.ctrl.CreateEntity();
    f.ctrl.Undo();
    f.ctrl.Redo();
    CHECK(f.ctrl.Hierarchy().size() == 1);
    CHECK_FALSE(f.ctrl.HasError());
}

TEST_CASE("EditorController:空栈 Undo → LastError、不崩") {
    Fixture f;
    CHECK_FALSE(f.ctrl.CanUndo());
    f.ctrl.Undo();
    CHECK(f.ctrl.HasError());
}
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cmake --build build --target me_tests`
Expected: 链接失败(`Undo`/`Redo`/`CanUndo`/`CanRedo` 未定义)。

- [ ] **Step 3: 实现**

```cpp
bool EditorController::CanUndo() const { return m_ctx.commands.canUndo(); }
bool EditorController::CanRedo() const { return m_ctx.commands.canRedo(); }

void EditorController::reconcileSelection() {
    if (!HasSelection()) {
        m_hasInspected = false;
        return;
    }
    for (const EntityRow& row : m_hierarchy) {
        if (row.id == m_selected) {
            InspectSelected();
            return;
        }
    }
    m_selected = kInvalidEntityId;
    m_hasInspected = false;
}

void EditorController::Undo() {
    const me::command::CommandResult res = m_ctx.commands.undo(m_ctx.scene);
    if (!res.ok) {
        m_lastError = "undo: " + res.message;
        return;
    }
    m_lastError.clear();
    RefreshHierarchy();   // 内部失败会写 LastError;此处成功路径不覆盖
    reconcileSelection();
}

void EditorController::Redo() {
    const me::command::CommandResult res = m_ctx.commands.redo(m_ctx.scene);
    if (!res.ok) {
        m_lastError = "redo: " + res.message;
        return;
    }
    m_lastError.clear();
    RefreshHierarchy();
    reconcileSelection();
}
```
(需在 `EditorController.cpp` 顶部 `#include "me/command/CommandResult.h"`;`CommandStack.h` 已由 `ToolContext.h` 间接可见,但显式包含 `me/command/CommandStack.h` 更稳。)

- [ ] **Step 4: 运行测试,确认通过**

Run: `cmake --build build --target me_tests && ./build/bin/me_tests -tc="EditorController:*"`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/editor/src/EditorController.cpp tests/editor/test_editor_controller.cpp
git commit -m "feat(editor): Undo/Redo 直调 CommandStack(spec §5 缺口)+ 选中核对"
```

---

## Task 7: RefreshLog

**Files:**
- Modify: `engine/editor/src/EditorController.cpp`
- Test: `tests/editor/test_editor_controller.cpp`

**Interfaces:**
- Consumes:`log.read` 入参 `{}`,返回 `{count, invocations:[{id, tool, params, dryRun, ok, code, message}]}`。
- Produces:`void RefreshLog()`——填充 `Log()`(`LogRow{invocationId, toolName, ok, code}`)。

- [ ] **Step 1: 写失败测试**

```cpp
TEST_CASE("EditorController:RefreshLog 反映此前工具调用审计") {
    Fixture f;
    f.ctrl.CreateEntity(); // 经 create_entity + list_entities + get_entity 多次 invoke
    f.ctrl.RefreshLog();
    REQUIRE_FALSE(f.ctrl.Log().empty());
    // 最近一条记录应有非空 toolName 与 invocationId 单调>0。
    const LogRow& last = f.ctrl.Log().back();
    CHECK(last.invocationId > 0);
    CHECK_FALSE(last.toolName.empty());
    CHECK_FALSE(f.ctrl.HasError());
}
```

- [ ] **Step 2: 运行测试,确认失败**

Run: `cmake --build build --target me_tests`
Expected: 链接失败(`RefreshLog` 未定义)。

- [ ] **Step 3: 实现**

```cpp
void EditorController::RefreshLog() {
    m_log.clear();
    const me::toolapi::ToolResult r = invoke("log.read", nlohmann::json::object());
    if (!r.ok || !r.data.contains("invocations")) return;
    for (const nlohmann::json& e : r.data["invocations"]) {
        LogRow row;
        row.invocationId = e.value("id", std::uint64_t{0});
        row.toolName = e.value("tool", std::string{});
        row.ok = e.value("ok", true);
        row.code = e.value("code", std::string{});
        m_log.push_back(std::move(row));
    }
}
```

- [ ] **Step 4: 运行测试,确认通过**

Run: `cmake --build build --target me_tests && ./build/bin/me_tests -tc="EditorController:*"`
Expected: 全部 PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/editor/src/EditorController.cpp tests/editor/test_editor_controller.cpp
git commit -m "feat(editor): RefreshLog(经 log.read)→ Log 面板数据源"
```

---

## Task 8: third_party 引入 Dear ImGui

**Files:**
- Modify: `third_party/CMakeLists.txt`

**Interfaces:**
- Produces:CMake target `imgui`(STATIC),含 imgui 核心 + `imgui_impl_dx12` + `imgui_impl_win32`;include 目录含 imgui 根与 `backends`。仅在 `WIN32` 下定义(后端依赖 d3d12/win32)。

> 本任务无 WSL 单测(ImGui 后端 Windows-only)。验证 = Windows 侧 CMake 配置成功 + 目标存在;沿用 nlohmann 的"URL 钉版避免全量 clone"策略(见 third-party 依赖记忆)。

- [ ] **Step 1: 追加 ImGui 声明**

在 `third_party/CMakeLists.txt` 末尾追加:
```cmake
# Dear ImGui:调试 GUI / ToolAPI 客户端(M7)。仅 Windows 构建(DX12+Win32 后端)。
# 用 release tarball URL 而非 git clone,避免全量历史(沿用 nlohmann 教训)。
if(WIN32)
    FetchContent_Declare(
        imgui
        URL https://github.com/ocornut/imgui/archive/refs/tags/v1.91.5.tar.gz
    )
    FetchContent_MakeAvailable(imgui)

    add_library(imgui STATIC
        ${imgui_SOURCE_DIR}/imgui.cpp
        ${imgui_SOURCE_DIR}/imgui_draw.cpp
        ${imgui_SOURCE_DIR}/imgui_tables.cpp
        ${imgui_SOURCE_DIR}/imgui_widgets.cpp
        ${imgui_SOURCE_DIR}/backends/imgui_impl_win32.cpp
        ${imgui_SOURCE_DIR}/backends/imgui_impl_dx12.cpp
    )
    target_include_directories(imgui PUBLIC
        ${imgui_SOURCE_DIR}
        ${imgui_SOURCE_DIR}/backends)
    target_compile_features(imgui PUBLIC cxx_std_17)
    # DX12 后端所需系统库。
    target_link_libraries(imgui PUBLIC d3d12 dxgi)
endif()
```

- [ ] **Step 2: Windows 侧配置验证**

Run(Windows / vcvars64,见 windows-dx12-toolchain 记忆):`cmake -B build -DME_BUILD_TESTS=ON`
Expected: 配置成功,无 "target imgui not found"。(WSL 侧因 `if(WIN32)` 跳过,正常。)

- [ ] **Step 3: 提交**

```bash
git add third_party/CMakeLists.txt
git commit -m "build(deps): 引入 Dear ImGui v1.91.5(Windows,DX12+Win32 后端)"
```

---

## Task 9: platform Window 增加 Win32 消息钩子

**Files:**
- Modify: `engine/platform/include/me/platform/Window.h`
- Modify: `engine/platform/src/`(Window 的 Win32 实现 .cpp;实现内 WndProc 处)

**Interfaces:**
- Produces:`using WndProcHook = bool(*)(void* hwnd, unsigned int msg, unsigned long long wparam, long long lparam);` 与 `void Window::SetWndProcHook(WndProcHook hook);`。WndProc 在分发前调用钩子;钩子返回 true 表示已消费(吞掉该消息)。签名用裸整型/void*,**platform 不依赖 ImGui**。

> 无 WSL 单测(Win32-only)。验证 = Task 10 联调时 ImGui 能响应鼠标/键盘。

- [ ] **Step 1: 头文件加钩子类型与注册**

在 `Window.h` 的 `SetInput` 声明后追加:
```cpp
    /// @brief Win32 消息钩子签名(裸类型,使 platform 不依赖 ImGui)。
    /// @return true 表示消息已被钩子消费(WndProc 不再继续默认处理)。
    using WndProcHook = bool (*)(void* hwnd, unsigned int msg,
                                 unsigned long long wparam, long long lparam);

    /// @brief 注册消息钩子(供 ImGui 等外部后端拦截窗口消息);传 nullptr 取消。
    void SetWndProcHook(WndProcHook hook);
```

- [ ] **Step 2: 实现内存钩子字段 + WndProc 调用**

在 Window 的 Win32 实现 .cpp 中:
- `Impl` 增加成员 `Window::WndProcHook wndProcHook = nullptr;`
- `Window::SetWndProcHook` 写入 `m_impl->wndProcHook = hook;`
- 在自由函数 WndProc 顶部(取到 `Window::Impl*` 后、处理本引擎消息前)加:
```cpp
    if (impl && impl->wndProcHook) {
        // 转为裸类型传出;ImGui 处理器在 sandbox 侧 reinterpret 回 HWND/WPARAM/LPARAM。
        if (impl->wndProcHook(hwnd, msg,
                              static_cast<unsigned long long>(wParam),
                              static_cast<long long>(lParam))) {
            return TRUE; // 钩子已消费
        }
    }
```
(具体取 `impl` 的方式沿用该 .cpp 既有 `GetWindowLongPtr` 模式;若 WndProc 在 `WM_NCCREATE` 前 impl 尚未绑定,保持原有空判分支不变。)

- [ ] **Step 3: Windows 侧编译验证**

Run(Windows):`cmake --build build --target me_platform`
Expected: 编译通过。

- [ ] **Step 4: 提交**

```bash
git add engine/platform
git commit -m "feat(platform): Window 增 Win32 消息钩子(裸类型,不依赖 ImGui)供 M7 ImGui 接入"
```

---

## Task 10: sandbox 接入 ImGui + 四面板(目视层)

**Files:**
- Modify: `sandbox/CMakeLists.txt`
- Modify: `sandbox/main.cpp`

**Interfaces:**
- Consumes:`me::editor::EditorController` 全部公开方法;`imgui` target;`Window::SetWndProcHook`;现有 `srvHeap`(`rhi::DescriptorHeap`,容量 8)。
- Produces:可运行的编辑器 sandbox(目视验证)。

> 无 WSL 单测(ImGui+DX12)。验证 = 真机目视(pending-user):面板出现、Create/Destroy/Undo 改变画面、Inspector 编辑变换生效、Log 显示调用链。ImGui DX12 API 按 v1.91.5 签名。

- [ ] **Step 1: CMake 链接**

`sandbox/CMakeLists.txt` 的 `target_link_libraries(sandbox PRIVATE ...)` 追加 `me_command me_toolapi me_editor imgui`:
```cmake
target_link_libraries(sandbox PRIVATE
    me_core me_platform me_rhi me_rhi_cpu me_renderer me_assets me_scene
    me_command me_toolapi me_editor imgui)
```

- [ ] **Step 2: main.cpp 头 + 后端转发**

在 `sandbox/main.cpp` 顶部 include 区追加:
```cpp
#include "me/command/CommandStack.h"
#include "me/toolapi/ToolRegistry.h"
#include "me/toolapi/ToolContext.h"
#include "me/toolapi/tools/BuiltinTools.h"
#include "me/editor/EditorController.h"

#include "imgui.h"
#include "backends/imgui_impl_win32.h"
#include "backends/imgui_impl_dx12.h"
#include <windows.h>
```
在匿名命名空间内追加 ImGui 的 Win32 处理器外部声明与钩子适配器:
```cpp
// ImGui 提供的 Win32 消息处理器(由其后端导出)。
extern IMGUI_IMPL_API LRESULT ImGui_ImplWin32_WndProcHandler(HWND, UINT, WPARAM, LPARAM);

// 适配 platform 的裸类型钩子签名 → ImGui 处理器。返回 true 表示已消费。
bool ImGuiWndProcHook(void* hwnd, unsigned int msg,
                      unsigned long long wparam, long long lparam) {
    return ImGui_ImplWin32_WndProcHandler(reinterpret_cast<HWND>(hwnd), msg,
                                          static_cast<WPARAM>(wparam),
                                          static_cast<LPARAM>(lparam)) != 0;
}
```

- [ ] **Step 3: 初始化 ImGui + 编辑器栈(在 batch 创建之后、主循环之前)**

```cpp
    // —— M7:ToolAPI + 编辑器栈 ——
    me::toolapi::ToolRegistry registry;
    me::toolapi::RegisterBuiltinTools(registry);
    me::command::CommandStack cmdStack;
    me::toolapi::ToolInvocationLog toolLog;
    me::toolapi::ToolContext toolCtx{scene, cmdStack, toolLog};
    me::editor::EditorController editor(registry, toolCtx);
    editor.RefreshHierarchy();

    // —— ImGui 初始化 —— 字体 SRV 用 srvHeap 末槽(容量 8,索引 7;tileset 用 0)。
    constexpr std::uint32_t kImGuiFontSrvIndex = 7;
    auto fontSrv = srvHeap->AllocateAt(kImGuiFontSrvIndex); // CPU+GPU 句柄,见下注
    IMGUI_CHECKVERSION();
    ImGui::CreateContext();
    ImGui::StyleColorsDark();
    ImGui_ImplWin32_Init(window->NativeHandle());
    ImGui_ImplDX12_Init(device->Device(), /*numFramesInFlight*/2,
                        DXGI_FORMAT_R8G8B8A8_UNORM, srvHeap->Heap(),
                        fontSrv.cpu, fontSrv.gpu);
    window->SetWndProcHook(&ImGuiWndProcHook);
    bool editorVisible = true;
```
> 注:`srvHeap->AllocateAt(index)` 若现有 `DescriptorHeap` 无此 API,本步顺带补一个返回 `{D3D12_CPU_DESCRIPTOR_HANDLE cpu; D3D12_GPU_DESCRIPTOR_HANDLE gpu;}` 的按索引分配方法(对照现有 `Allocate()` 实现,偏移 `index * 描述符增量`);GPU 句柄需堆为 shader-visible(srvHeap 创建时 `true`,已满足)。交换链格式若非 `R8G8B8A8_UNORM`,改为与 `SwapChain` 一致的格式常量。

- [ ] **Step 4: 主循环内接入 ImGui 帧 + 面板**

在主循环 `input.NewFrame(); window->PumpMessages();` 之后、游戏逻辑之前不变;在"系统更新"之前插入 ImGui 帧与面板绘制:
```cpp
        // —— ImGui 新帧 + 编辑器面板(变更经 EditorController → Tool API)——
        ImGui_ImplDX12_NewFrame();
        ImGui_ImplWin32_NewFrame();
        ImGui::NewFrame();
        if (input.WasPressed(platform::KeyCode::Grave)) editorVisible = !editorVisible;
        if (editorVisible) {
            // Hierarchy
            ImGui::Begin("Hierarchy");
            if (ImGui::Button("Create")) { editor.CreateEntity(); }
            ImGui::SameLine();
            if (ImGui::Button("Destroy") && editor.HasSelection()) { editor.DestroySelected(); }
            ImGui::Separator();
            for (const auto& row : editor.Hierarchy()) {
                char label[32];
                std::snprintf(label, sizeof(label), "Entity #%u",
                              static_cast<unsigned>(row.id));
                if (ImGui::Selectable(label, editor.Selected() == row.id)) {
                    editor.Select(row.id);
                    editor.InspectSelected();
                }
            }
            ImGui::End();

            // Inspector
            ImGui::Begin("Inspector");
            if (editor.HasInspected()) {
                me::Transform2D t = editor.Inspected().localTransform;
                float pos[2] = {t.position.x, t.position.y};
                float scl[2] = {t.scale.x, t.scale.y};
                bool changed = false;
                changed |= ImGui::DragFloat2("position", pos, 1.0f);
                changed |= ImGui::DragFloat("rotation", &t.rotation, 0.01f);
                changed |= ImGui::DragFloat2("scale", scl, 0.01f);
                if (changed) {
                    t.position = me::Vector2{pos[0], pos[1]};
                    t.scale = me::Vector2{scl[0], scl[1]};
                    editor.ApplyTransform(t);
                }
            } else {
                ImGui::TextUnformatted("(no selection)");
            }
            ImGui::Separator();
            if (ImGui::Button("Undo") && editor.CanUndo()) editor.Undo();
            ImGui::SameLine();
            if (ImGui::Button("Redo") && editor.CanRedo()) editor.Redo();
            ImGui::End();

            // Stats(运行时遥测,非 Tool 数据,见 spec §1.2 边界)
            ImGui::Begin("Stats");
            ImGui::Text("entities: %d", static_cast<int>(editor.Hierarchy().size()));
            ImGui::Text("frame: %.2f ms", 1000.0f / ImGui::GetIO().Framerate);
            ImGui::End();

            // Log
            ImGui::Begin("Log");
            if (ImGui::Button("Refresh")) editor.RefreshLog();
            ImGui::Separator();
            for (const auto& lr : editor.Log()) {
                ImGui::Text("#%llu %s [%s]",
                            static_cast<unsigned long long>(lr.invocationId),
                            lr.toolName.c_str(), lr.code.c_str());
            }
            ImGui::End();

            // 错误条
            if (editor.HasError()) {
                ImGui::Begin("Editor Error");
                ImGui::TextColored(ImVec4(1, 0.4f, 0.4f, 1), "%s",
                                   editor.LastError().c_str());
                if (ImGui::Button("Clear")) editor.ClearError();
                ImGui::End();
            }
        }
```
> `KeyCode::Grave`(` 键)若 `platform::KeyCode` 未定义,改用一个已存在的键(如 `KeyCode::Tab`)或本步顺带在 InputState 加该键码。

- [ ] **Step 5: 录制 ImGui 绘制到命令列表(在 `batch->End(cmd);` 之后、转换回 PRESENT 之前)**

```cpp
        // ImGui 录到同一命令列表(SRV 堆已 Set;ImGui 字体 SRV 在同一 srvHeap)。
        ImGui::Render();
        ImGui_ImplDX12_RenderDrawData(ImGui::GetDrawData(), cmd);
```

- [ ] **Step 6: 关闭时清理(主循环后、return 前,`fence->Flush` 之后)**

```cpp
    ImGui_ImplDX12_Shutdown();
    ImGui_ImplWin32_Shutdown();
    ImGui::DestroyContext();
```

- [ ] **Step 7: Windows 侧构建 + 目视验证**

Run(Windows):`cmake --build build --target sandbox` 后运行 `build/bin/sandbox.exe`
Expected(pending-user 目视):四面板显示;Create 加实体并出现在 Hierarchy;选中后 Inspector 拖动 position 实体在画面移动;Destroy 移除;Undo/Redo 往返;Log Refresh 显示工具调用链;`` ` `` 切换编辑器可见。WASD 仍移动 player(运行时直调路径不受影响)。

- [ ] **Step 8: 提交**

```bash
git add sandbox/CMakeLists.txt sandbox/main.cpp engine/rhi
git commit -m "feat(sandbox): 接入 ImGui + 四面板编辑器(全变更经 EditorController→ToolAPI)"
```

---

## Task 11: ADR 0005 + PROGRESS 回写

**Files:**
- Create: `docs/architecture/0005-m7-editor.md`
- Modify: `docs/PROGRESS.md`

> 文档任务,无测试。

- [ ] **Step 1: 写 ADR 0005**

`docs/architecture/0005-m7-editor.md`,记录:精简切片决策(只用 6 Tool)、EditorController 读写都走 Tool 的反向验证立场、ImGui 封死 sandbox 边界、Undo/Redo 直调 CommandStack 的缺口、get_entity 无组件/无 label 两处发现及后续候选 Tool(`edit.undo`/`edit.redo`、`entity.set_name`、get_entity 增 components + `entity.add_component`)。

- [ ] **Step 2: 回写 PROGRESS.md**

更新"当前阶段"为 M7 完成 / 下一步 M8;M7 行状态改 ☑ 并写一句话现状(me_editor + EditorController 经 6 Tool 驱动四面板 + 反向验证发现);追加关键决策记录行;在"下一步行动"换为 M8 农场领域层 + 后续 Tool 候选(由 M7 发现驱动)。

- [ ] **Step 3: 提交**

```bash
git add docs/architecture/0005-m7-editor.md docs/PROGRESS.md
git commit -m "docs(progress): M7 Editor as Client 完成回写 + ADR 0005"
```

---

## Self-Review

**Spec 覆盖核对:**
- §1.2 读写都走 Tool → Task 1-5,7(查询/变更全经 `invoke()`→`ToolRegistry`)。✅
- §1.3 只用 6 Tool、零序列化 → 全计划无新 Tool / 无 save-load。✅
- §3.1 EditorController 持 Registry&/Context& → Task 1 构造。✅
- §3.2 七意图方法映射 → RefreshHierarchy(T1)/InspectSelected(T2)/CreateEntity(T3)/ApplyTransform(T4)/DestroySelected(T5)/Undo·Redo(T6)/RefreshLog(T7)。✅
- §3.3 DTO 三结构对齐实际 Tool 输出 → Task 1 EditorDto.h。✅
- §3.4 错误不静默 → `invoke()` 写 LastError(T1)+ 多任务错误用例(T2/T4/T5/T6)。✅
- §4 sandbox 集成(后端/消息钩子/每帧序/四面板/可开关)→ Task 8/9/10。✅
- §5 两/三处发现 → Undo/Redo 直调(T6)+ ADR 记录(T11)。✅
- §6.1 测试矩阵 → Task 1-7 用例逐条覆盖(Refresh/Select/Inspect/Create/Apply/Destroy/Undo/错误/Log)。✅
- §7 交付物清单 → 全任务文件覆盖含 README(T1)/ADR+PROGRESS(T11)。✅

**Placeholder 扫描:** 无 TBD/TODO;每个代码步给出完整代码。Task 3/9/10 中对"若现有 API 缺失则顺带补"的两处(`AllocateAt`、`KeyCode::Grave`)给了明确兜底指引,非占位。✅

**类型一致性:** `kInvalidEntityId`、`HasSelection/Selected`、`Inspected/HasInspected`、`Hierarchy/Log` 访问器、`invoke()`/`reconcileSelection()` 私有助手在 T1 头文件统一声明,后续任务签名一致;DTO 字段名(`localTransform`/`parentId`/`invocationId`/`toolName`)前后一致。✅
