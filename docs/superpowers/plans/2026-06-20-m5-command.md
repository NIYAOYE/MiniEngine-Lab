# M5 Command 中枢 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 MiniEngine 加入可回滚的 Command 中枢(`ICommand` + `CommandStack` + Undo/Redo),并落地 Create/Destroy/SetTransform 三个具体命令。

**Architecture:** 新增 `engine/command/` 静态库(单向依赖 `me_scene`→`me_core`)。命令永不持有裸 `Entity` handle,改持有持久 `EntityId`,在 execute/undo 时 `Resolve` 为当前存活 handle —— 因此销毁后重建(generation 变化)也不悬垂。`DestroyEntityCmd` 通过类型擦除的组件快照 + 子树前序遍历实现全状态还原。

**Tech Stack:** C++17、CMake(≥3.20,模块化 STATIC 库)、doctest(CPU-only 单测,WSL 红绿)。

## Global Constraints

- C++17;每模块一个 STATIC 库 target,依赖严格单向(`command → scene → core`,严禁反向)。
- 无硬编码数值:所有常量用具名 `constexpr` 或配置。
- 不使用 C++ 异常:可恢复错误用 `CommandResult` 返回值;不变量违反用 `ME_ASSERT`/`ME_ASSERT_MSG`。
- 公开 API 写 Doxygen 注释;非显而易见实现写行内注释。
- 禁止 Singleton / 全局可变状态;Scene 显式传参。头文件中禁止 `using namespace std`。
- 新增模块必须同步更新 `CMakeLists.txt`(根 + tests)与模块 `README.md`。
- 逻辑层与渲染层解耦:`me_command` 与 `me_scene` 均为 CPU-only,可脱离窗口/DX12 单测。
- 测试框架:doctest,中文 `TEST_CASE` 名,沿用 `tests/<module>/test_*.cpp` 约定。

---

## File Structure

**新增:**
- `engine/command/CMakeLists.txt` — `me_command` 静态库定义
- `engine/command/README.md` — 模块说明
- `engine/command/include/me/command/CommandResult.h` — `CommandResult` 值类型
- `engine/command/include/me/command/ICommand.h` — `ICommand` 接口
- `engine/command/include/me/command/CommandStack.h` — `CommandStack` 双栈
- `engine/command/src/CommandStack.cpp`
- `engine/command/include/me/command/commands/CreateEntityCmd.h`
- `engine/command/src/CreateEntityCmd.cpp`
- `engine/command/include/me/command/commands/SetTransformCmd.h`
- `engine/command/src/SetTransformCmd.cpp`
- `engine/command/include/me/command/commands/DestroyEntityCmd.h`
- `engine/command/src/DestroyEntityCmd.cpp`
- `tests/scene/test_entity_id.cpp` — Scene EntityId/Resolve/CreateEntityWithId 单测
- `tests/scene/test_component_snapshot.cpp` — 组件快照单测
- `tests/command/test_command_stack.cpp` — CommandStack 语义单测
- `tests/command/test_create_entity_cmd.cpp`
- `tests/command/test_set_transform_cmd.cpp`
- `tests/command/test_destroy_entity_cmd.cpp`

**修改:**
- `engine/scene/include/me/scene/Scene.h` — 加 EntityId 类型 + 身份/恢复 API + 快照 API
- `engine/scene/src/Scene.cpp` — 实现上述
- `engine/scene/include/me/scene/ComponentStorage.h` — 加 `IComponentSnapshot` + `Capture`
- `CMakeLists.txt`(根)— `add_subdirectory(engine/command)`
- `tests/CMakeLists.txt` — 新增测试源 + 链接 `me_command`
- `docs/PROGRESS.md` — M5 勾选 + 决策记录
- `docs/architecture/0003-m5-command.md` — ADR 摘要(新增)

> 设计说明:为避免 `Scene.h ↔ ComponentStorage.h` 头文件循环,`IComponentSnapshot::RestoreTo` 改为只接收 `Entity`(通过快照内持有的 `ComponentStorage<T>*` 回写),不回调 `Scene` —— 与 spec 等价,且自包含于 `ComponentStorage.h`。相应 `Capture`/`CaptureComponents` 为非 const(销毁命令本就在非 const Scene 上工作)。

---

## Task 1: Scene EntityId 身份锚定

为 `Scene` 增加持久 `EntityId`(单调递增、永不复用)、`IdOf`/`Resolve` 解析,以及命令专用的 `CreateEntityWithId` 恢复路径。

**Files:**
- Modify: `engine/scene/include/me/scene/Scene.h`
- Modify: `engine/scene/src/Scene.cpp`
- Test: `tests/scene/test_entity_id.cpp`
- Modify: `tests/CMakeLists.txt`

**Interfaces:**
- Produces:
  - `using me::scene::EntityId = std::uint64_t;`(0 = 无效)
  - `Entity Scene::CreateEntity();`(行为扩展:额外分配并登记新 `EntityId`)
  - `EntityId Scene::IdOf(Entity e) const;`(失活返回 0)
  - `Entity Scene::Resolve(EntityId id) const;`(无存活对应返回 `Entity::Invalid()`)
  - `Entity Scene::CreateEntityWithId(EntityId id);`(命令恢复路径,以原 id 重建)

- [ ] **Step 1: 写失败测试**

创建 `tests/scene/test_entity_id.cpp`:

```cpp
#include <doctest/doctest.h>

#include "me/scene/Scene.h"

using me::scene::Entity;
using me::scene::EntityId;
using me::scene::Scene;

TEST_CASE("Scene:EntityId 单调递增且与句柄互相解析") {
    Scene scene;
    const Entity a = scene.CreateEntity();
    const Entity b = scene.CreateEntity();
    const EntityId ida = scene.IdOf(a);
    const EntityId idb = scene.IdOf(b);
    CHECK(ida != 0);
    CHECK(idb != 0);
    CHECK(ida != idb);
    CHECK(scene.Resolve(ida) == a);
    CHECK(scene.Resolve(idb) == b);
}

TEST_CASE("Scene:销毁后 IdOf 归零、Resolve 失效") {
    Scene scene;
    const Entity a = scene.CreateEntity();
    const EntityId ida = scene.IdOf(a);
    scene.DestroyEntity(a);
    CHECK(scene.IdOf(a) == 0);
    CHECK_FALSE(scene.Resolve(ida).IsValid());
}

TEST_CASE("Scene:CreateEntityWithId 以原 id 重建——身份锚定不随 generation 变化") {
    Scene scene;
    const Entity a = scene.CreateEntity();
    const EntityId ida = scene.IdOf(a);
    scene.DestroyEntity(a);

    const Entity restored = scene.CreateEntityWithId(ida);
    CHECK(scene.IsAlive(restored));
    CHECK(scene.IdOf(restored) == ida);     // 同一逻辑身份
    CHECK(scene.Resolve(ida) == restored);  // 解析到新句柄
    CHECK(restored != a);                    // generation 已变,handle 不同
}

TEST_CASE("Scene:CreateEntityWithId 后续 CreateEntity 不与其 id 冲突") {
    Scene scene;
    const Entity a = scene.CreateEntity();
    const EntityId ida = scene.IdOf(a);
    scene.DestroyEntity(a);
    scene.CreateEntityWithId(ida);
    const Entity c = scene.CreateEntity();
    CHECK(scene.IdOf(c) != ida);
}
```

- [ ] **Step 2: 运行测试确认失败**

先把测试源加入构建。编辑 `tests/CMakeLists.txt`,在 `scene/test_entity.cpp` 行后插入:

```cmake
    scene/test_entity_id.cpp
```

Run:
```bash
cmake --build build --target me_tests 2>&1 | tail -20
```
Expected: 编译失败 —— `IdOf` / `Resolve` / `CreateEntityWithId` / `EntityId` 未声明。

- [ ] **Step 3: 实现 Scene.h 声明**

在 `engine/scene/include/me/scene/Scene.h` 顶部 `namespace me::scene {` 后、`class Scene` 前加类型别名:

```cpp
/// @brief 持久实体身份:单调递增、永不复用;命令用它锚定实体,跨销毁/重建稳定。0 表示无效。
using EntityId = std::uint64_t;
```

在 `class Scene` 的 `// —— 层级与变换 ——` 区块之前(`CreateEntity` 声明附近)加成员声明:

```cpp
    /// @brief 取实体的持久身份 id;实体失活返回 0。
    EntityId IdOf(Entity e) const;
    /// @brief 把持久身份解析为当前存活句柄;无对应存活实体返回 Entity::Invalid()。
    Entity Resolve(EntityId id) const;
    /// @brief 命令恢复路径:以给定 id 重建实体(用于 create 的 redo、destroy 的 undo)。
    ///        前置:id != 0 且当前不存活;generation 正常推进,id 重新登记。
    Entity CreateEntityWithId(EntityId id);
```

在 `struct Slot` 内(`generation` 字段旁)加:

```cpp
        EntityId id = 0;               // 持久身份(0 = 无)
```

在 private 数据成员区(`m_activeCamera` 附近)加:

```cpp
    EntityId m_nextId = 1;             // 单调 id 分配器(0 保留为无效)
    std::unordered_map<EntityId, std::uint32_t> m_idToIndex; // 存活 id → 槽位 index
```

- [ ] **Step 4: 实现 Scene.cpp**

在 `engine/scene/src/Scene.cpp` 的 `CreateEntity()` 中,`++m_aliveCount;` 之前插入 id 分配:

```cpp
    s.id = m_nextId++;
    m_idToIndex.emplace(s.id, index);
```

在 `DestroyEntity()` 中,设置 `slot.alive = false;` 之前插入 id 注销:

```cpp
    m_idToIndex.erase(slot.id);
    slot.id = 0;
```

在文件中 `bool Scene::IsAlive` 定义之后,追加三个新方法:

```cpp
Scene::EntityId Scene::IdOf(Entity e) const {
    const Slot* s = SlotOf(e);
    return s ? s->id : 0;
}

Entity Scene::Resolve(EntityId id) const {
    if (id == 0) return Entity::Invalid();
    auto it = m_idToIndex.find(id);
    if (it == m_idToIndex.end()) return Entity::Invalid();
    const std::uint32_t index = it->second;
    return Entity{index, m_slots[index].generation};
}

Entity Scene::CreateEntityWithId(EntityId id) {
    ME_ASSERT_MSG(id != 0, "CreateEntityWithId: id 不能为 0");
    ME_ASSERT_MSG(m_idToIndex.find(id) == m_idToIndex.end(),
                  "CreateEntityWithId: id 当前已存活");
    std::uint32_t index;
    if (!m_freeList.empty()) {
        index = m_freeList.back();
        m_freeList.pop_back();
    } else {
        index = static_cast<std::uint32_t>(m_slots.size());
        m_slots.emplace_back();
    }
    Slot& s = m_slots[index];
    s.alive = true;
    s.local = me::Transform2D{};
    s.parent = Entity::Invalid();
    s.children.clear();
    s.world = me::Matrix4x4{};
    s.worldDirty = true;
    s.id = id;
    m_idToIndex.emplace(id, index);
    if (id >= m_nextId) m_nextId = id + 1; // 保证后续分配不与恢复的 id 冲突
    ++m_aliveCount;
    return Entity{index, s.generation};
}
```

> 注:`Scene.h` 已 `#include <cstdint>`、`<unordered_map>`,无需新增 include。返回类型 `Scene::EntityId` 因 `EntityId` 在 `me::scene` 命名空间内,定义处用 `Entity`/`Scene::EntityId` 均可解析。

- [ ] **Step 5: 运行测试确认通过**

Run:
```bash
cmake --build build --target me_tests 2>&1 | tail -5 && ./build/tests/me_tests -ts=* -tc="Scene:EntityId*","Scene:销毁后 IdOf*","Scene:CreateEntityWithId*"
```
Expected: 新增 4 个 TEST_CASE 全 PASS;原有 Scene 测试不回归。

- [ ] **Step 6: 提交**

```bash
git add engine/scene/include/me/scene/Scene.h engine/scene/src/Scene.cpp tests/scene/test_entity_id.cpp tests/CMakeLists.txt
git commit -m "feat(scene): 持久 EntityId 身份锚定 + Resolve/CreateEntityWithId(M5 前置)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 组件快照(类型擦除)

为类型擦除的组件存储增加只读快照,使 `DestroyEntityCmd` 的 undo 能还原任意组件,且不让命令直接触碰 Scene 私有存储。

**Files:**
- Modify: `engine/scene/include/me/scene/ComponentStorage.h`
- Modify: `engine/scene/include/me/scene/Scene.h`
- Modify: `engine/scene/src/Scene.cpp`
- Test: `tests/scene/test_component_snapshot.cpp`
- Modify: `tests/CMakeLists.txt`

**Interfaces:**
- Consumes(Task 1):`Scene`、`Entity`。
- Produces:
  - `class me::scene::IComponentSnapshot { virtual void RestoreTo(Entity) const = 0; };`
  - `std::unique_ptr<IComponentSnapshot> IComponentStorage::Capture(Entity);`(纯虚)
  - `std::vector<std::unique_ptr<IComponentSnapshot>> Scene::CaptureComponents(Entity e);`
  - `void Scene::RestoreComponents(Entity e, const std::vector<std::unique_ptr<IComponentSnapshot>>& snaps);`

- [ ] **Step 1: 写失败测试**

创建 `tests/scene/test_component_snapshot.cpp`:

```cpp
#include <doctest/doctest.h>

#include "me/scene/Components.h"
#include "me/scene/Scene.h"

using me::scene::CameraComponent;
using me::scene::Entity;
using me::scene::Scene;
using me::scene::SpriteComponent;

TEST_CASE("Scene:CaptureComponents/RestoreComponents 跨实体还原组件数据") {
    Scene scene;
    const Entity src = scene.CreateEntity();
    SpriteComponent sprite;
    sprite.textureId = 7u;
    sprite.sortLayer = 3;
    scene.AddComponent<SpriteComponent>(src, sprite);
    CameraComponent cam;
    cam.zoom = 2.5f;
    scene.AddComponent<CameraComponent>(src, cam);

    auto snaps = scene.CaptureComponents(src);
    CHECK(snaps.size() == 2);

    const Entity dst = scene.CreateEntity();
    scene.RestoreComponents(dst, snaps);

    REQUIRE(scene.HasComponent<SpriteComponent>(dst));
    REQUIRE(scene.HasComponent<CameraComponent>(dst));
    CHECK(scene.GetComponent<SpriteComponent>(dst)->textureId == 7u);
    CHECK(scene.GetComponent<SpriteComponent>(dst)->sortLayer == 3);
    CHECK(scene.GetComponent<CameraComponent>(dst)->zoom == doctest::Approx(2.5f));
}

TEST_CASE("Scene:无组件实体 CaptureComponents 为空") {
    Scene scene;
    const Entity e = scene.CreateEntity();
    CHECK(scene.CaptureComponents(e).empty());
}
```

- [ ] **Step 2: 运行测试确认失败**

编辑 `tests/CMakeLists.txt`,在 `scene/test_entity_id.cpp` 行后插入:

```cmake
    scene/test_component_snapshot.cpp
```

Run:
```bash
cmake --build build --target me_tests 2>&1 | tail -20
```
Expected: 编译失败 —— `CaptureComponents` / `RestoreComponents` 未声明。

- [ ] **Step 3: 实现 ComponentStorage.h 快照**

编辑 `engine/scene/include/me/scene/ComponentStorage.h`。在文件顶部 `#include` 区确保有 `#include <memory>`(若无则添加)。

把 `IComponentStorage` 替换为带快照接口的版本,并在其前声明 `IComponentSnapshot`、在其后定义 `ComponentSnapshot<T>` 与 `ComponentStorage<T>::Capture`。具体改动:

(1) 在 `class IComponentStorage` 之前插入:

```cpp
/// @brief 单个组件的只读快照:可把所记录的组件值重新写回某实体(供命令 Undo 还原)。
class IComponentSnapshot {
public:
    virtual ~IComponentSnapshot() = default;
    /// @brief 把快照里的组件值添加到实体 e(写回到产生本快照的存储)。
    virtual void RestoreTo(Entity e) const = 0;
};

template <class T>
class ComponentStorage; // 前置声明,供 Capture 返回 ComponentSnapshot<T>
```

(2) 在 `class IComponentStorage` 内,`virtual bool Has(Entity e) const = 0;` 之后加:

```cpp
    /// @brief 若实体拥有该组件,返回其快照;否则返回 nullptr。
    virtual std::unique_ptr<IComponentSnapshot> Capture(Entity e) = 0;
```

(3) 在 `ComponentStorage<T>` 类内,`void Remove(Entity e) override { ... }` 之后加声明:

```cpp
    std::unique_ptr<IComponentSnapshot> Capture(Entity e) override;
```

(4) 在 `ComponentStorage<T>` 类定义(`};`)之后、`} // namespace me::scene` 之前,加快照实现:

```cpp
/// @brief 持有某组件值拷贝的快照;RestoreTo 时写回创建它的存储实例。
template <class T>
class ComponentSnapshot final : public IComponentSnapshot {
public:
    ComponentSnapshot(ComponentStorage<T>* store, const T& value)
        : m_store(store), m_value(value) {}
    void RestoreTo(Entity e) const override { m_store->Add(e, m_value); }

private:
    ComponentStorage<T>* m_store; // 非拥有:存储归 Scene,生命周期长于快照
    T m_value;
};

template <class T>
std::unique_ptr<IComponentSnapshot> ComponentStorage<T>::Capture(Entity e) {
    auto it = m_sparse.find(e.index);
    if (it == m_sparse.end() || m_owners[it->second] != e) return nullptr;
    return std::make_unique<ComponentSnapshot<T>>(this, m_items[it->second]);
}
```

- [ ] **Step 4: 实现 Scene 快照聚合 API**

在 `engine/scene/include/me/scene/Scene.h` 的「组件」区块(`RemoveComponent` 之后)加声明:

```cpp
    /// @brief 收集实体当前全部组件的快照(供命令快照实体完整状态)。
    std::vector<std::unique_ptr<IComponentSnapshot>> CaptureComponents(Entity e);
    /// @brief 把一组组件快照还原到实体 e。
    void RestoreComponents(
        Entity e, const std::vector<std::unique_ptr<IComponentSnapshot>>& snaps);
```

确保 `Scene.h` 顶部有 `#include <memory>`(已有)。在 `engine/scene/src/Scene.cpp` 的 `RemoveAllComponents` 之后追加:

```cpp
std::vector<std::unique_ptr<IComponentSnapshot>> Scene::CaptureComponents(Entity e) {
    std::vector<std::unique_ptr<IComponentSnapshot>> out;
    for (auto& kv : m_stores) {
        if (auto snap = kv.second->Capture(e)) out.push_back(std::move(snap));
    }
    return out;
}

void Scene::RestoreComponents(
    Entity e, const std::vector<std::unique_ptr<IComponentSnapshot>>& snaps) {
    for (const auto& snap : snaps) snap->RestoreTo(e);
}
```

- [ ] **Step 5: 运行测试确认通过**

Run:
```bash
cmake --build build --target me_tests 2>&1 | tail -5 && ./build/tests/me_tests -tc="Scene:CaptureComponents*","Scene:无组件实体*"
```
Expected: 2 个 TEST_CASE PASS;无回归。

- [ ] **Step 6: 提交**

```bash
git add engine/scene/include/me/scene/ComponentStorage.h engine/scene/include/me/scene/Scene.h engine/scene/src/Scene.cpp tests/scene/test_component_snapshot.cpp tests/CMakeLists.txt
git commit -m "feat(scene): 类型擦除组件快照 Capture/Restore(M5 前置)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: command 模块骨架 + CommandResult + ICommand + CommandStack

建立 `me_command` 静态库,定义结果类型、命令接口、标准 Undo/Redo 双栈,并接入构建与测试。用一个测试内的假命令验证栈语义。

**Files:**
- Create: `engine/command/CMakeLists.txt`, `engine/command/README.md`
- Create: `engine/command/include/me/command/CommandResult.h`
- Create: `engine/command/include/me/command/ICommand.h`
- Create: `engine/command/include/me/command/CommandStack.h`
- Create: `engine/command/src/CommandStack.cpp`
- Modify: `CMakeLists.txt`(根)
- Modify: `tests/CMakeLists.txt`
- Test: `tests/command/test_command_stack.cpp`

**Interfaces:**
- Consumes(Task 1):`me::scene::Scene`。
- Produces:
  - `struct me::command::CommandResult { bool ok; std::string message; static CommandResult Ok(std::string={}); static CommandResult Fail(std::string); };`
  - `class me::command::ICommand { virtual CommandResult execute(me::scene::Scene&)=0; virtual CommandResult undo(me::scene::Scene&)=0; virtual std::string describe() const=0; };`
  - `class me::command::CommandStack { CommandResult execute(std::unique_ptr<ICommand>, me::scene::Scene&); CommandResult undo(me::scene::Scene&); CommandResult redo(me::scene::Scene&); bool canUndo() const; bool canRedo() const; std::size_t undoDepth() const; std::size_t redoDepth() const; void clear(); };`

- [ ] **Step 1: 写失败测试**

创建 `tests/command/test_command_stack.cpp`:

```cpp
#include <doctest/doctest.h>

#include "me/command/CommandStack.h"
#include "me/command/ICommand.h"
#include "me/scene/Scene.h"

using me::command::CommandResult;
using me::command::CommandStack;
using me::command::ICommand;
using me::scene::Scene;

namespace {
/// 假命令:execute +1,undo -1,写入共享计数器,验证栈调度。
struct CounterCmd final : ICommand {
    int* counter;
    bool failExecute;
    explicit CounterCmd(int* c, bool fail = false) : counter(c), failExecute(fail) {}
    CommandResult execute(Scene&) override {
        if (failExecute) return CommandResult::Fail("故意失败");
        ++(*counter);
        return CommandResult::Ok();
    }
    CommandResult undo(Scene&) override {
        --(*counter);
        return CommandResult::Ok();
    }
    std::string describe() const override { return "counter"; }
};
} // namespace

TEST_CASE("CommandStack:execute 改状态并入 undo 栈") {
    Scene scene;
    CommandStack stack;
    int n = 0;
    const CommandResult r = stack.execute(std::make_unique<CounterCmd>(&n), scene);
    CHECK(r.ok);
    CHECK(n == 1);
    CHECK(stack.canUndo());
    CHECK_FALSE(stack.canRedo());
    CHECK(stack.undoDepth() == 1);
}

TEST_CASE("CommandStack:undo/redo 往返") {
    Scene scene;
    CommandStack stack;
    int n = 0;
    stack.execute(std::make_unique<CounterCmd>(&n), scene);
    CHECK(stack.undo(scene).ok);
    CHECK(n == 0);
    CHECK(stack.canRedo());
    CHECK(stack.redo(scene).ok);
    CHECK(n == 1);
    CHECK_FALSE(stack.canRedo());
}

TEST_CASE("CommandStack:新 execute 清空 redo 栈") {
    Scene scene;
    CommandStack stack;
    int n = 0;
    stack.execute(std::make_unique<CounterCmd>(&n), scene);
    stack.undo(scene);
    CHECK(stack.redoDepth() == 1);
    stack.execute(std::make_unique<CounterCmd>(&n), scene); // 应清空 redo
    CHECK(stack.redoDepth() == 0);
    CHECK_FALSE(stack.canRedo());
}

TEST_CASE("CommandStack:空栈 undo/redo 返回失败") {
    Scene scene;
    CommandStack stack;
    CHECK_FALSE(stack.undo(scene).ok);
    CHECK_FALSE(stack.redo(scene).ok);
}

TEST_CASE("CommandStack:execute 失败的命令不入栈、不改状态") {
    Scene scene;
    CommandStack stack;
    int n = 0;
    const CommandResult r =
        stack.execute(std::make_unique<CounterCmd>(&n, /*fail=*/true), scene);
    CHECK_FALSE(r.ok);
    CHECK(n == 0);
    CHECK_FALSE(stack.canUndo());
    CHECK(stack.undoDepth() == 0);
}
```

- [ ] **Step 2: 创建模块文件**

创建 `engine/command/include/me/command/CommandResult.h`:

```cpp
#pragma once

#include <string>
#include <utility>

namespace me::command {

/**
 * @brief 命令执行结果(不抛异常)。可恢复失败以 ok=false + message 返回。
 *
 * M6 会把它包装为 JSON 边界的 ToolResult;M5 阶段仅人读消息。
 */
struct CommandResult {
    bool ok = true;
    std::string message;

    /// @brief 构造成功结果(可附带说明)。
    static CommandResult Ok(std::string msg = {}) { return {true, std::move(msg)}; }
    /// @brief 构造失败结果(必须给出原因)。
    static CommandResult Fail(std::string msg) { return {false, std::move(msg)}; }
};

} // namespace me::command
```

创建 `engine/command/include/me/command/ICommand.h`:

```cpp
#pragma once

#include <string>

#include "me/command/CommandResult.h"

namespace me::scene {
class Scene;
}

namespace me::command {

/**
 * @brief 可回滚命令接口。
 *
 * execute 用于首次执行与 redo(二者语义一致);undo 撤销;describe 供 dry-run 预览。
 * 实现者承诺:undo 能把场景恢复到 execute 之前的等价状态。
 */
class ICommand {
public:
    virtual ~ICommand() = default;
    /// @brief 执行(或重做)。失败返回 ok=false 且不应产生部分副作用。
    virtual CommandResult execute(me::scene::Scene& scene) = 0;
    /// @brief 撤销最近一次 execute。
    virtual CommandResult undo(me::scene::Scene& scene) = 0;
    /// @brief 返回人读的命令描述,供 dry-run/日志。
    virtual std::string describe() const = 0;
};

} // namespace me::command
```

创建 `engine/command/include/me/command/CommandStack.h`:

```cpp
#pragma once

#include <cstddef>
#include <memory>
#include <vector>

#include "me/command/CommandResult.h"
#include "me/command/ICommand.h"

namespace me::scene {
class Scene;
}

namespace me::command {

/**
 * @brief 标准 Undo/Redo 双栈。
 *
 * 语义:execute 成功才压入 undo 栈并清空 redo 栈;undo/redo 在两栈间移动。
 * 不做命令合并、不设容量上限(YAGNI,留待交互编辑器里程碑)。
 */
class CommandStack {
public:
    /// @brief 执行命令;成功则接管所有权入 undo 栈并清空 redo;失败则丢弃命令、不入栈。
    CommandResult execute(std::unique_ptr<ICommand> cmd, me::scene::Scene& scene);
    /// @brief 撤销栈顶命令;undo 栈空返回失败。
    CommandResult undo(me::scene::Scene& scene);
    /// @brief 重做最近撤销的命令;redo 栈空返回失败。
    CommandResult redo(me::scene::Scene& scene);

    bool canUndo() const { return !m_undo.empty(); }
    bool canRedo() const { return !m_redo.empty(); }
    std::size_t undoDepth() const { return m_undo.size(); }
    std::size_t redoDepth() const { return m_redo.size(); }
    /// @brief 清空两栈(丢弃全部历史)。
    void clear();

private:
    std::vector<std::unique_ptr<ICommand>> m_undo;
    std::vector<std::unique_ptr<ICommand>> m_redo;
};

} // namespace me::command
```

创建 `engine/command/src/CommandStack.cpp`:

```cpp
#include "me/command/CommandStack.h"

namespace me::command {

CommandResult CommandStack::execute(std::unique_ptr<ICommand> cmd,
                                    me::scene::Scene& scene) {
    CommandResult r = cmd->execute(scene);
    if (!r.ok) return r;          // 失败命令不入栈,无副作用承诺由命令保证
    m_undo.push_back(std::move(cmd));
    m_redo.clear();               // 新分支:历史 redo 失效
    return r;
}

CommandResult CommandStack::undo(me::scene::Scene& scene) {
    if (m_undo.empty()) return CommandResult::Fail("undo 栈为空");
    // 先执行再移动:失败则命令留在 undo 栈,便于诊断。
    CommandResult r = m_undo.back()->undo(scene);
    if (!r.ok) return r;
    m_redo.push_back(std::move(m_undo.back()));
    m_undo.pop_back();
    return r;
}

CommandResult CommandStack::redo(me::scene::Scene& scene) {
    if (m_redo.empty()) return CommandResult::Fail("redo 栈为空");
    CommandResult r = m_redo.back()->execute(scene);
    if (!r.ok) return r;
    m_undo.push_back(std::move(m_redo.back()));
    m_redo.pop_back();
    return r;
}

void CommandStack::clear() {
    m_undo.clear();
    m_redo.clear();
}

} // namespace me::command
```

创建 `engine/command/CMakeLists.txt`:

```cmake
add_library(me_command STATIC
    src/CommandStack.cpp
)
target_include_directories(me_command PUBLIC include)
target_compile_features(me_command PUBLIC cxx_std_17)
# 单向依赖:command → scene → core。严禁依赖 rhi / renderer。
target_link_libraries(me_command PUBLIC me_scene)
```

创建 `engine/command/README.md`:

```markdown
# me_command — Command 中枢(M5)

可回滚变更基础设施:`ICommand`(execute/undo/describe)+ `CommandStack`(标准 Undo/Redo 双栈)+ 具体命令(CreateEntity / DestroyEntity / SetTransform)。

- **CPU-only**:仅依赖 `me_scene` → `me_core`,可脱离 DX12/窗口单测。
- **身份锚定**:命令持有 `me::scene::EntityId` 而非裸 handle,在 execute/undo 时 `Scene::Resolve` 为存活句柄,跨销毁/重建稳定。
- **错误模型**:不抛异常,以 `CommandResult { ok, message }` 返回;execute 失败不入栈。
- **后续(M6)**:Tool API 将变更型 Tool 经 `CommandStack` 执行,并把 `CommandResult` 包装为 JSON `ToolResult`。
```

- [ ] **Step 3: 接入构建**

编辑根 `CMakeLists.txt`,在 `add_subdirectory(engine/scene)` 之后加:

```cmake
add_subdirectory(engine/command)
```

编辑 `tests/CMakeLists.txt`:在 `scene/test_component_snapshot.cpp` 行后加测试源:

```cmake
    command/test_command_stack.cpp
```
并在 `target_link_libraries(me_tests PRIVATE ...)` 行末尾加 `me_command`:

```cmake
target_link_libraries(me_tests PRIVATE doctest::doctest me_core me_platform me_rhi_cpu me_assets me_scene me_command)
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```bash
cmake -S . -B build > /dev/null && cmake --build build --target me_tests 2>&1 | tail -5 && ./build/tests/me_tests -tc="CommandStack:*"
```
Expected: 5 个 CommandStack TEST_CASE 全 PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/command CMakeLists.txt tests/CMakeLists.txt tests/command/test_command_stack.cpp
git commit -m "feat(command): me_command 模块 + ICommand/CommandResult/CommandStack(M5)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: CreateEntityCmd

第一个具体命令:创建实体,undo 销毁,redo 以原 `EntityId` 重建。

**Files:**
- Create: `engine/command/include/me/command/commands/CreateEntityCmd.h`
- Create: `engine/command/src/CreateEntityCmd.cpp`
- Modify: `engine/command/CMakeLists.txt`
- Test: `tests/command/test_create_entity_cmd.cpp`
- Modify: `tests/CMakeLists.txt`

**Interfaces:**
- Consumes:`ICommand`、`CommandStack`、`Scene::CreateEntity/CreateEntityWithId/IdOf/Resolve/DestroyEntity`。
- Produces:`class me::command::CreateEntityCmd final : public ICommand;`,默认构造;`me::scene::EntityId CreatedId() const;`(便于测试与链式命令取得新实体身份)。

- [ ] **Step 1: 写失败测试**

创建 `tests/command/test_create_entity_cmd.cpp`:

```cpp
#include <doctest/doctest.h>

#include "me/command/CommandStack.h"
#include "me/command/commands/CreateEntityCmd.h"
#include "me/scene/Scene.h"

using me::command::CommandStack;
using me::command::CreateEntityCmd;
using me::scene::EntityId;
using me::scene::Scene;

TEST_CASE("CreateEntityCmd:execute 创建、undo 销毁、redo 以原 id 重建") {
    Scene scene;
    CommandStack stack;
    auto cmd = std::make_unique<CreateEntityCmd>();
    CreateEntityCmd* raw = cmd.get();

    CHECK(stack.execute(std::move(cmd), scene).ok);
    const EntityId id = raw->CreatedId();
    CHECK(id != 0);
    CHECK(scene.IsAlive(scene.Resolve(id)));
    CHECK(scene.AliveCount() == 1);

    CHECK(stack.undo(scene).ok);
    CHECK_FALSE(scene.Resolve(id).IsValid());
    CHECK(scene.AliveCount() == 0);

    CHECK(stack.redo(scene).ok);
    CHECK(scene.IsAlive(scene.Resolve(id)));     // 同一逻辑身份回来
    CHECK(raw->CreatedId() == id);                // id 稳定
    CHECK(scene.AliveCount() == 1);
}
```

- [ ] **Step 2: 运行测试确认失败**

编辑 `tests/CMakeLists.txt`,在 `command/test_command_stack.cpp` 行后加:

```cmake
    command/test_create_entity_cmd.cpp
```

Run:
```bash
cmake -S . -B build > /dev/null && cmake --build build --target me_tests 2>&1 | tail -20
```
Expected: 编译失败 —— `CreateEntityCmd` 头文件不存在。

- [ ] **Step 3: 实现命令**

创建 `engine/command/include/me/command/commands/CreateEntityCmd.h`:

```cpp
#pragma once

#include <string>

#include "me/command/ICommand.h"
#include "me/scene/Scene.h"

namespace me::command {

/**
 * @brief 创建一个新实体(局部变换为单位、无父)。
 *
 * execute 首次创建并记录其 EntityId;redo 以同一 EntityId 重建;undo 销毁。
 */
class CreateEntityCmd final : public ICommand {
public:
    CommandResult execute(me::scene::Scene& scene) override;
    CommandResult undo(me::scene::Scene& scene) override;
    std::string describe() const override;

    /// @brief 已创建实体的持久身份(execute 后有效;未执行为 0)。
    me::scene::EntityId CreatedId() const { return m_id; }

private:
    me::scene::EntityId m_id = 0; // 0 = 尚未首次执行
};

} // namespace me::command
```

创建 `engine/command/src/CreateEntityCmd.cpp`:

```cpp
#include "me/command/commands/CreateEntityCmd.h"

namespace me::command {

CommandResult CreateEntityCmd::execute(me::scene::Scene& scene) {
    if (m_id == 0) {
        // 首次执行:分配新实体并记录其持久身份。
        const me::scene::Entity e = scene.CreateEntity();
        m_id = scene.IdOf(e);
    } else {
        // redo:以原身份重建,保证后续命令对该 id 的引用仍然成立。
        scene.CreateEntityWithId(m_id);
    }
    return CommandResult::Ok("创建实体 #" + std::to_string(m_id));
}

CommandResult CreateEntityCmd::undo(me::scene::Scene& scene) {
    const me::scene::Entity e = scene.Resolve(m_id);
    if (!e.IsValid()) return CommandResult::Fail("CreateEntityCmd::undo 实体已失效");
    scene.DestroyEntity(e);
    return CommandResult::Ok();
}

std::string CreateEntityCmd::describe() const {
    return "创建实体 #" + std::to_string(m_id);
}

} // namespace me::command
```

编辑 `engine/command/CMakeLists.txt`,在 `add_library(me_command STATIC` 列表中加源文件:

```cmake
    src/CreateEntityCmd.cpp
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```bash
cmake -S . -B build > /dev/null && cmake --build build --target me_tests 2>&1 | tail -5 && ./build/tests/me_tests -tc="CreateEntityCmd:*"
```
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/command/include/me/command/commands/CreateEntityCmd.h engine/command/src/CreateEntityCmd.cpp engine/command/CMakeLists.txt tests/command/test_create_entity_cmd.cpp tests/CMakeLists.txt
git commit -m "feat(command): CreateEntityCmd(execute/undo/redo 以 EntityId 锚定)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: SetTransformCmd

修改实体局部变换,undo 还原旧值。

**Files:**
- Create: `engine/command/include/me/command/commands/SetTransformCmd.h`
- Create: `engine/command/src/SetTransformCmd.cpp`
- Modify: `engine/command/CMakeLists.txt`
- Test: `tests/command/test_set_transform_cmd.cpp`
- Modify: `tests/CMakeLists.txt`

**Interfaces:**
- Consumes:`ICommand`、`Scene::Resolve/LocalTransform/SetLocalTransform`、`me::Transform2D`、`me::scene::EntityId`。
- Produces:`class me::command::SetTransformCmd final : public ICommand;`,构造 `SetTransformCmd(me::scene::EntityId target, const me::Transform2D& newLocal);`。

- [ ] **Step 1: 写失败测试**

创建 `tests/command/test_set_transform_cmd.cpp`:

```cpp
#include <doctest/doctest.h>

#include "me/command/CommandStack.h"
#include "me/command/commands/SetTransformCmd.h"
#include "me/core/Transform2D.h"
#include "me/scene/Scene.h"

using me::command::CommandStack;
using me::command::SetTransformCmd;
using me::scene::Entity;
using me::scene::EntityId;
using me::scene::Scene;

TEST_CASE("SetTransformCmd:execute 改变、undo 还原、redo 再变") {
    Scene scene;
    CommandStack stack;
    const Entity e = scene.CreateEntity();
    const EntityId id = scene.IdOf(e);

    me::Transform2D next;
    next.position = me::Vector2{10.0f, 20.0f};
    next.rotation = 1.5f;

    CHECK(stack.execute(std::make_unique<SetTransformCmd>(id, next), scene).ok);
    CHECK(scene.LocalTransform(e).position.x == doctest::Approx(10.0f));
    CHECK(scene.LocalTransform(e).rotation == doctest::Approx(1.5f));

    CHECK(stack.undo(scene).ok);
    CHECK(scene.LocalTransform(e).position.x == doctest::Approx(0.0f));
    CHECK(scene.LocalTransform(e).rotation == doctest::Approx(0.0f));

    CHECK(stack.redo(scene).ok);
    CHECK(scene.LocalTransform(e).position.y == doctest::Approx(20.0f));
}

TEST_CASE("SetTransformCmd:对失活实体 execute 返回失败") {
    Scene scene;
    CommandStack stack;
    const Entity e = scene.CreateEntity();
    const EntityId id = scene.IdOf(e);
    scene.DestroyEntity(e);

    me::Transform2D next;
    next.position = me::Vector2{5.0f, 5.0f};
    const auto r = stack.execute(std::make_unique<SetTransformCmd>(id, next), scene);
    CHECK_FALSE(r.ok);
    CHECK_FALSE(stack.canUndo());
}
```

- [ ] **Step 2: 运行测试确认失败**

编辑 `tests/CMakeLists.txt`,在 `command/test_create_entity_cmd.cpp` 行后加:

```cmake
    command/test_set_transform_cmd.cpp
```

Run:
```bash
cmake -S . -B build > /dev/null && cmake --build build --target me_tests 2>&1 | tail -20
```
Expected: 编译失败 —— `SetTransformCmd` 头文件不存在。

- [ ] **Step 3: 实现命令**

创建 `engine/command/include/me/command/commands/SetTransformCmd.h`:

```cpp
#pragma once

#include <string>

#include "me/command/ICommand.h"
#include "me/core/Transform2D.h"
#include "me/scene/Scene.h"

namespace me::command {

/**
 * @brief 设置实体的局部变换;undo 还原首次执行前的旧值。
 */
class SetTransformCmd final : public ICommand {
public:
    SetTransformCmd(me::scene::EntityId target, const me::Transform2D& newLocal)
        : m_target(target), m_new(newLocal) {}

    CommandResult execute(me::scene::Scene& scene) override;
    CommandResult undo(me::scene::Scene& scene) override;
    std::string describe() const override;

private:
    me::scene::EntityId m_target;
    me::Transform2D m_new;
    me::Transform2D m_old{};       // 首次 execute 时缓存
    bool m_captured = false;       // 是否已缓存旧值
};

} // namespace me::command
```

创建 `engine/command/src/SetTransformCmd.cpp`:

```cpp
#include "me/command/commands/SetTransformCmd.h"

namespace me::command {

CommandResult SetTransformCmd::execute(me::scene::Scene& scene) {
    const me::scene::Entity e = scene.Resolve(m_target);
    if (!e.IsValid()) return CommandResult::Fail("SetTransformCmd 目标实体已失效");
    if (!m_captured) {
        m_old = scene.LocalTransform(e); // 仅首次执行缓存旧值,redo 复用
        m_captured = true;
    }
    scene.SetLocalTransform(e, m_new);
    return CommandResult::Ok(describe());
}

CommandResult SetTransformCmd::undo(me::scene::Scene& scene) {
    const me::scene::Entity e = scene.Resolve(m_target);
    if (!e.IsValid()) return CommandResult::Fail("SetTransformCmd::undo 目标实体已失效");
    scene.SetLocalTransform(e, m_old);
    return CommandResult::Ok();
}

std::string SetTransformCmd::describe() const {
    return "设置实体 #" + std::to_string(m_target) + " 局部变换";
}

} // namespace me::command
```

编辑 `engine/command/CMakeLists.txt`,在源列表加:

```cmake
    src/SetTransformCmd.cpp
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```bash
cmake -S . -B build > /dev/null && cmake --build build --target me_tests 2>&1 | tail -5 && ./build/tests/me_tests -tc="SetTransformCmd:*"
```
Expected: 2 个 TEST_CASE PASS。

- [ ] **Step 5: 提交**

```bash
git add engine/command/include/me/command/commands/SetTransformCmd.h engine/command/src/SetTransformCmd.cpp engine/command/CMakeLists.txt tests/command/test_set_transform_cmd.cpp tests/CMakeLists.txt
git commit -m "feat(command): SetTransformCmd(execute 缓存旧值/undo 还原)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: DestroyEntityCmd(全子树 + 组件 + active camera 还原)

最硬的 undo:销毁实体及其整棵子树,undo 按原 `EntityId` 逐个重建,还原局部变换、父子层级、组件,以及 active camera。

**Files:**
- Create: `engine/command/include/me/command/commands/DestroyEntityCmd.h`
- Create: `engine/command/src/DestroyEntityCmd.cpp`
- Modify: `engine/command/CMakeLists.txt`
- Test: `tests/command/test_destroy_entity_cmd.cpp`
- Modify: `tests/CMakeLists.txt`

**Interfaces:**
- Consumes:`ICommand`、`Scene::Resolve/IdOf/Children/Parent/LocalTransform/SetLocalTransform/SetParent/DestroyEntity/CreateEntityWithId/CaptureComponents/RestoreComponents/ActiveCamera/SetActiveCamera`、`IComponentSnapshot`、`me::scene::EntityId`、`me::Transform2D`。
- Produces:`class me::command::DestroyEntityCmd final : public ICommand;`,构造 `explicit DestroyEntityCmd(me::scene::EntityId target);`。

- [ ] **Step 1: 写失败测试**

创建 `tests/command/test_destroy_entity_cmd.cpp`:

```cpp
#include <doctest/doctest.h>

#include "me/command/CommandStack.h"
#include "me/command/commands/DestroyEntityCmd.h"
#include "me/scene/Components.h"
#include "me/scene/Scene.h"

using me::command::CommandStack;
using me::command::DestroyEntityCmd;
using me::scene::CameraComponent;
using me::scene::Entity;
using me::scene::EntityId;
using me::scene::Scene;
using me::scene::SpriteComponent;

TEST_CASE("DestroyEntityCmd:undo 还原子树/层级/局部变换/组件/active camera") {
    Scene scene;
    CommandStack stack;

    // 父 - 子 - 孙;父带 Sprite,子设为 active camera 带 CameraComponent。
    const Entity parent = scene.CreateEntity();
    const Entity child = scene.CreateEntity();
    const Entity grand = scene.CreateEntity();
    scene.SetParent(child, parent);
    scene.SetParent(grand, child);

    me::Transform2D ct;
    ct.position = me::Vector2{4.0f, 6.0f};
    scene.SetLocalTransform(child, ct);

    SpriteComponent sprite;
    sprite.textureId = 9u;
    scene.AddComponent<SpriteComponent>(parent, sprite);
    CameraComponent cam;
    cam.zoom = 3.0f;
    scene.AddComponent<CameraComponent>(child, cam);
    scene.SetActiveCamera(child);

    const EntityId pid = scene.IdOf(parent);
    const EntityId cid = scene.IdOf(child);
    const EntityId gid = scene.IdOf(grand);

    CHECK(stack.execute(std::make_unique<DestroyEntityCmd>(pid), scene).ok);
    CHECK(scene.AliveCount() == 0);
    CHECK_FALSE(scene.ActiveCamera().IsValid());

    CHECK(stack.undo(scene).ok);
    CHECK(scene.AliveCount() == 3);

    const Entity p2 = scene.Resolve(pid);
    const Entity c2 = scene.Resolve(cid);
    const Entity g2 = scene.Resolve(gid);
    REQUIRE(p2.IsValid());
    REQUIRE(c2.IsValid());
    REQUIRE(g2.IsValid());

    // 层级还原
    CHECK(scene.Parent(c2) == p2);
    CHECK(scene.Parent(g2) == c2);
    // 局部变换还原
    CHECK(scene.LocalTransform(c2).position.x == doctest::Approx(4.0f));
    // 组件还原
    REQUIRE(scene.HasComponent<SpriteComponent>(p2));
    CHECK(scene.GetComponent<SpriteComponent>(p2)->textureId == 9u);
    REQUIRE(scene.HasComponent<CameraComponent>(c2));
    CHECK(scene.GetComponent<CameraComponent>(c2)->zoom == doctest::Approx(3.0f));
    // active camera 还原
    CHECK(scene.ActiveCamera() == c2);
}

TEST_CASE("DestroyEntityCmd:redo 再次销毁后二次 undo 仍正确") {
    Scene scene;
    CommandStack stack;
    const Entity parent = scene.CreateEntity();
    const Entity child = scene.CreateEntity();
    scene.SetParent(child, parent);
    const EntityId pid = scene.IdOf(parent);
    const EntityId cid = scene.IdOf(child);

    stack.execute(std::make_unique<DestroyEntityCmd>(pid), scene);
    stack.undo(scene);
    CHECK(stack.redo(scene).ok);            // 再次销毁
    CHECK(scene.AliveCount() == 0);
    CHECK(stack.undo(scene).ok);            // 二次还原
    CHECK(scene.AliveCount() == 2);
    CHECK(scene.Parent(scene.Resolve(cid)) == scene.Resolve(pid));
}
```

- [ ] **Step 2: 运行测试确认失败**

编辑 `tests/CMakeLists.txt`,在 `command/test_set_transform_cmd.cpp` 行后加:

```cmake
    command/test_destroy_entity_cmd.cpp
```

Run:
```bash
cmake -S . -B build > /dev/null && cmake --build build --target me_tests 2>&1 | tail -20
```
Expected: 编译失败 —— `DestroyEntityCmd` 头文件不存在。

- [ ] **Step 3: 实现命令**

创建 `engine/command/include/me/command/commands/DestroyEntityCmd.h`:

```cpp
#pragma once

#include <memory>
#include <string>
#include <vector>

#include "me/command/ICommand.h"
#include "me/core/Transform2D.h"
#include "me/scene/ComponentStorage.h"
#include "me/scene/Scene.h"

namespace me::command {

/**
 * @brief 销毁实体及其整棵子树;undo 按原 EntityId 还原层级/变换/组件/active camera。
 *
 * execute 先前序快照子树(父先于子),再销毁;每次 execute 都刷新快照,
 * 因此 redo→undo 仍一致。
 */
class DestroyEntityCmd final : public ICommand {
public:
    explicit DestroyEntityCmd(me::scene::EntityId target) : m_target(target) {}

    CommandResult execute(me::scene::Scene& scene) override;
    CommandResult undo(me::scene::Scene& scene) override;
    std::string describe() const override;

private:
    // 单个实体的完整快照(身份 + 局部变换 + 父身份 + 组件)。
    struct NodeSnapshot {
        me::scene::EntityId id = 0;
        me::scene::EntityId parentId = 0; // 0 = 无父
        me::Transform2D local{};
        std::vector<std::unique_ptr<me::scene::IComponentSnapshot>> comps;
    };

    me::scene::EntityId m_target;
    std::vector<NodeSnapshot> m_nodes;        // 前序:父先于子
    me::scene::EntityId m_activeCameraId = 0; // 被销毁子树内的 active camera(0 = 不在子树内)
};

} // namespace me::command
```

创建 `engine/command/src/DestroyEntityCmd.cpp`:

```cpp
#include "me/command/commands/DestroyEntityCmd.h"

#include <vector>

namespace me::command {

CommandResult DestroyEntityCmd::execute(me::scene::Scene& scene) {
    const me::scene::Entity root = scene.Resolve(m_target);
    if (!root.IsValid()) return CommandResult::Fail("DestroyEntityCmd 目标实体已失效");

    m_nodes.clear();
    m_activeCameraId = 0;
    const me::scene::Entity activeCam = scene.ActiveCamera();
    const me::scene::EntityId activeCamId = scene.IdOf(activeCam);

    // 前序栈遍历:父先于子入列(保证 undo 时父先重建)。
    std::vector<me::scene::Entity> stack{root};
    while (!stack.empty()) {
        const me::scene::Entity cur = stack.back();
        stack.pop_back();
        NodeSnapshot node;
        node.id = scene.IdOf(cur);
        node.parentId = scene.IdOf(scene.Parent(cur));
        node.local = scene.LocalTransform(cur);
        node.comps = scene.CaptureComponents(cur);
        if (activeCamId != 0 && node.id == activeCamId) m_activeCameraId = activeCamId;
        m_nodes.push_back(std::move(node));
        // 逆序压栈使弹出顺序与 children 顺序一致(前序稳定)。
        const auto& kids = scene.Children(cur);
        for (auto it = kids.rbegin(); it != kids.rend(); ++it) stack.push_back(*it);
    }

    scene.DestroyEntity(root); // 连带销毁整棵子树
    return CommandResult::Ok(describe());
}

CommandResult DestroyEntityCmd::undo(me::scene::Scene& scene) {
    // 父先于子:逐个以原 id 重建,再连父子、还原变换与组件。
    for (auto& node : m_nodes) {
        const me::scene::Entity e = scene.CreateEntityWithId(node.id);
        if (node.parentId != 0) {
            const me::scene::Entity parent = scene.Resolve(node.parentId);
            if (parent.IsValid()) scene.SetParent(e, parent);
        }
        scene.SetLocalTransform(e, node.local);
        scene.RestoreComponents(e, node.comps);
    }
    if (m_activeCameraId != 0) {
        const me::scene::Entity cam = scene.Resolve(m_activeCameraId);
        if (cam.IsValid()) scene.SetActiveCamera(cam);
    }
    return CommandResult::Ok();
}

std::string DestroyEntityCmd::describe() const {
    return "销毁实体 #" + std::to_string(m_target) + " 及其子树(" +
           std::to_string(m_nodes.size()) + " 个实体)";
}

} // namespace me::command
```

编辑 `engine/command/CMakeLists.txt`,在源列表加:

```cmake
    src/DestroyEntityCmd.cpp
```

- [ ] **Step 4: 运行测试确认通过**

Run:
```bash
cmake -S . -B build > /dev/null && cmake --build build --target me_tests 2>&1 | tail -5 && ./build/tests/me_tests -tc="DestroyEntityCmd:*"
```
Expected: 2 个 TEST_CASE PASS。

- [ ] **Step 5: 全量测试 + 提交**

Run:
```bash
./build/tests/me_tests 2>&1 | tail -5
```
Expected: 全部 TEST_CASE PASS(无回归)。

```bash
git add engine/command/include/me/command/commands/DestroyEntityCmd.h engine/command/src/DestroyEntityCmd.cpp engine/command/CMakeLists.txt tests/command/test_destroy_entity_cmd.cpp tests/CMakeLists.txt
git commit -m "feat(command): DestroyEntityCmd(全子树+组件+active camera 还原)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 进度回写 + ADR

更新跨会话进度与架构决策记录。纯文档任务。

**Files:**
- Modify: `docs/PROGRESS.md`
- Create: `docs/architecture/0003-m5-command.md`

- [ ] **Step 1: 更新 PROGRESS.md**

在「里程碑进度」表把 `M5 Command 中枢` 一行状态从 `☐` 改为 `☑`,说明改为:`ICommand + CommandStack(标准 Undo/Redo)+ CreateEntity/Destroy/SetTransform;EntityId 身份锚定 + 类型擦除组件快照;CPU-only doctest 全绿`。

更新文件头:
- `最后更新` 改为当前日期。
- `当前阶段` 改为:`M5 Command 中枢完成,下一步 M6 ToolAPI`。

更新「一句话现状」末尾追加一句 M5 总结。

更新「下一步行动」为指向 M6(ToolRegistry + Tool + JSON Schema 校验 + dry-run + 首批 Tool + 白名单),并提示对 M6 调用 `writing-plans`。

在「关键决策记录(ADR 摘要)」表追加四行(日期=当前日期):
- 命令以持久 `EntityId` 锚定身份(非裸 handle),execute/undo 时 `Resolve` → 跨销毁/重建稳定,并为序列化铺路。
- M5 命令范围 = Create/Destroy/SetTransform;PaintTile/AddComponent/SaveScene 随对应能力推迟。
- CommandStack 标准双栈,新 execute 清空 redo;不做合并/容量上限(YAGNI)。
- 组件快照 `RestoreTo(Entity)` 经存储指针回写(不回调 Scene),避免 `Scene.h↔ComponentStorage.h` 头文件循环。

- [ ] **Step 2: 写 ADR**

创建 `docs/architecture/0003-m5-command.md`,内容覆盖:背景(M5 目标)、决策(上述四点 + 选型理由)、后果(M6 如何在其上包装 ToolResult、命令合并/容量留待 M7)。参照 `docs/architecture/0002-m4-scene-components.md` 的现有格式与篇幅。

- [ ] **Step 3: 提交**

```bash
git add docs/PROGRESS.md docs/architecture/0003-m5-command.md
git commit -m "docs(progress): M5 Command 中枢完成回写 + ADR 0003

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage:**
- §2 模块结构 → Task 3(CMakeLists/README/库定义)。✓
- §3.1 EntityId/IdOf/Resolve/CreateEntityWithId → Task 1。✓
- §3.2 IComponentSnapshot/Capture/CaptureComponents/RestoreComponents → Task 2。✓
- §4 CommandResult/ICommand/CommandStack(含 undo/redo 失败留栈语义)→ Task 3。✓
- §5.1 CreateEntityCmd → Task 4;§5.2 SetTransformCmd → Task 5;§5.3 DestroyEntityCmd(子树/组件/active camera)→ Task 6。✓
- §6 测试与验收 → 各任务 TDD 步骤 + Task 6 全量回归。✓
- §7 不做项 → 计划未引入 JSON/Tool/合并/容量/sandbox。✓

**2. Placeholder scan:** 无 TBD/TODO;每个代码步骤含完整、可直接粘贴的代码。Task 6 子树遍历内联在 `execute`(前序栈),无冗余/占位块。

**3. Type consistency:**
- `EntityId`(`std::uint64_t`)在 Task 1 定义,Task 2/4/5/6 一致使用。
- `CommandResult::Ok/Fail`、`ICommand::execute/undo/describe` 签名跨 Task 3–6 一致。
- `CommandStack::execute/undo/redo/canUndo/canRedo/undoDepth/redoDepth/clear` 在 Task 3 定义并被后续测试调用,一致。
- `Scene` 新增方法名(`IdOf/Resolve/CreateEntityWithId/CaptureComponents/RestoreComponents`)在 Task 1/2 定义,Task 4–6 一致引用。
- `IComponentSnapshot::RestoreTo(Entity)` 在 Task 2 定义,Task 6 经 `Scene::RestoreComponents` 间接使用,一致。

无遗漏、无类型不一致。
