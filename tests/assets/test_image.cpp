#include <doctest/doctest.h>
#include <cstdint>
#include <vector>

#include "me/assets/ImageData.h"

using me::assets::DecodeImageRGBA8;

// 一个最小的合法 2x2 PNG(红/绿/蓝渐变),以字节数组内嵌,避免依赖磁盘文件。
// 真实 PNG 字节流:8-bit RGB,每扫描行 1 个过滤字节 + w 个像素(zlib 压缩)。
static const std::vector<std::uint8_t> kPng2x2 = {
    0x89, 0x50, 0x4E, 0x47, 0x0D, 0x0A, 0x1A, 0x0A, 0x00, 0x00, 0x00, 0x0D, 0x49, 0x48, 0x44, 0x52,
    0x00, 0x00, 0x00, 0x02, 0x00, 0x00, 0x00, 0x02, 0x08, 0x02, 0x00, 0x00, 0x00, 0xFD, 0xD4, 0x9A,
    0x73, 0x00, 0x00, 0x00, 0x14, 0x49, 0x44, 0x41, 0x54, 0x78, 0x9C, 0x63, 0x60, 0x00, 0x82, 0x8A,
    0x00, 0x86, 0x0A, 0x86, 0x80, 0x8A, 0x8A, 0x05, 0x00, 0x0D, 0xCE, 0x03, 0x21, 0x01, 0x03, 0xF3,
    0x4C, 0x00, 0x00, 0x00, 0x00, 0x49, 0x45, 0x4E, 0x44, 0xAE, 0x42, 0x60, 0x82,
};

TEST_CASE("DecodeImageRGBA8 解出 2x2 RGBA") {
    auto img = DecodeImageRGBA8(kPng2x2.data(), kPng2x2.size());
    REQUIRE(img.has_value());
    CHECK(img->width == 2);
    CHECK(img->height == 2);
    CHECK(img->pixels.size() == static_cast<size_t>(2 * 2 * 4));
    CHECK(img->IsValid());
}

TEST_CASE("非法字节返回 nullopt") {
    const std::vector<std::uint8_t> garbage = {0, 1, 2, 3, 4};
    auto img = DecodeImageRGBA8(garbage.data(), garbage.size());
    CHECK_FALSE(img.has_value());
}
